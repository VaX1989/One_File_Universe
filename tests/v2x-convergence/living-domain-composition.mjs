import assert from 'node:assert/strict';
import fs from 'node:fs';
import vm from 'node:vm';

const calls={deep:0,base:0,system:0,geography:0,terrain:0,surface:0,surface3d:0,local:0,local3d:0,matter:0,micro:0,matter3d:0,camera:0,lastTerrainArgs:null,activated:[],points:[],objects:[]};
function gradient(){return {addColorStop(){}}}
function context(){return {clearRect(){},createRadialGradient:gradient,createLinearGradient:gradient,fillRect(){},beginPath(){},closePath(){},arc(){},ellipse(){},moveTo(){},lineTo(){},stroke(){},fill(){},strokeRect(){},setLineDash(){},fillText(){},save(){},restore(){},set font(v){},set fillStyle(v){},set strokeStyle(v){},set lineWidth(v){},set textAlign(v){}}}
const ctx=context();
const canvas={width:900,height:620,clientWidth:900,clientHeight:620,getContext(){return ctx},getBoundingClientRect(){return {width:900,height:620}}};
const glCanvas={};
const camera={snapshot(){calls.camera++;return {pose:{orientation:[0,0,0,1]}}}};
let baseToken=0,baseReadyRevision=-1,releaseDeferred=null;
const baseRenderer={cameraAuthority:camera,async render(s){calls.base++;const token=++baseToken;if(s.defer)await new Promise(resolve=>{releaseDeferred=resolve});if(token!==baseToken)return;baseReadyRevision=s.revision},renderDeep3DScene(stage,factory){if(stage==='HUMAN')calls.local3d++;else if(['MATERIAL','MICROSTRUCTURE','MOLECULAR','ATOMIC'].includes(stage))calls.matter3d++;else calls.surface3d++;const scene=factory({position:[1,2,3]});return {sceneFingerprint:scene.fingerprint,packetCount:scene.packets.length,v2x13:{drawCalls:scene.packets.length}}},activateAt(){return false},keyboard(){return false},rotate(){},setTravelDistance(){},resize(){},sampleColor(){},dispose(){},state(){return {version:'base-renderer',readyRevision:baseReadyRevision,cameraAuthority:'v2x02-camera',metrics:{drawnObjects:2}}}};
const planet={kind:'planet',canonicalId:'planet-a',metadata:{facts:{baselineSemiMajorAxisMicroAu:'1000000',baselineEccentricityPpm:'10000',baselineInclinationMilliDeg:'1200'}}};
const star={kind:'star',canonicalId:'star-a',metadata:{facts:{baselineTemperatureK:'5772',baselineRadiusMilliSolar:'1000',baselineLuminosityMilliSolar:'1000'}}};
const point=Object.freeze({planetIdentity:'planet-a',locationIdentity:'loc-a',latMicroDeg:12000000,lonMicroDeg:24000000});
const planetology=Object.freeze({planetIdentity:'planet-a',bulkPriorClass:'TERRESTRIAL',hydrosphere:{waterAreaPpm:420000,iceFractionPpm:120000},surfaceProcesses:{tectonicActivityPpm:510000,erosionPotentialPpm:330000,impactActivityPpm:90000},interior:{volcanismPpm:280000},climate:{aridityPpm:260000},authority:'MODEL_DERIVED_SIMULATION'});
const exactSource=Object.freeze({contract:'ofu-v1-micro-source-1',sourceEntityId:'material-source-a'});
const surfaceFrame=Object.freeze({status:'READY',authority:'PRESENTATION_ONLY',projection:{bounds:[-.2,.2,-.18,.18]},summary:{patches:9,polygons:256,coastlines:4,rivers:3},truncated:{patches:0,polygons:0,coastlines:0,rivers:0}});
const O={
 v2x05DeepPlanetProvider:Object.freeze({query(p,context){calls.deep++;assert.equal(context,'SURFACE_PROMPT08_CONTEXT');return {supported:true,status:'PRESENT',modelDigest:'deep-test-digest',authority:'MODEL_DERIVED_SIMULATION',provenance:{modelId:'v2x05.model.deep-planet'},fidelity:{uncertainty:'Heuristic scenario depth only'},payload:{v2x06GeographyAdapter:{safeInputs:{tectonicActivityPpm:123000,volcanicActivityPpm:456000},withheldInputs:[{field:'waterAreaPpm'}]}}}}}),
 v1LivingRenderer:Object.freeze({VERSION:'base',create(){return baseRenderer}}),
 v2x02LivingCameraComposition:Object.freeze({orientationAngles(){return {yaw:.2,pitch:-.1}}}),
 v2x04SystemProvider:Object.freeze({VERSION:'ofu-v2x-04-system-provider-2',render(input,{camera,viewport,approach}){calls.system++;assert.equal(input.system.id,'system-a');assert.equal(input.planets[0].id,'planet-a');assert.equal(input.stars[0].id,'star-a');assert.equal(viewport.width,900);assert.equal(camera.aspect,900/620);if(approach){assert.equal(approach.bodyId,'planet-a');assert.equal(approach.surfaceTarget.latitudeDeg,12);assert.equal(approach.surfaceTarget.longitudeDeg,24)}return {providerId:'v2x04.system.presentation',authority:'PRESENTATION_ONLY',legacyFallbackUsed:false,selectionAuthority:'UPSTREAM_SELECTION_CONTRACT',semanticScaleAuthority:'EXTERNAL_CONVERGENCE_SCALE',projectedOrbits:[{canonicalEntityId:'planet-a',points:[{x:410,y:300},{x:450,y:280},{x:490,y:300}]}],hitTargets:[{canonicalEntityId:'star-a',kind:'STAR',x:430,y:300,depth:80,visible:true,radiusPx:9},{canonicalEntityId:'planet-a',kind:'PLANET',x:500,y:320,depth:100,visible:true,radiusPx:7}],starPresentations:[{canonicalEntityId:'star-a',renderCue:{discRadiusPx:8,glowIntensity:.8,srgb:[255,230,190]}}],resourceUsage:{stars:1,planets:1,projectedVertices:3,hitTargets:2},scene:{planets:[{orbit:{}}]},approach:approach?{roundTrip:{reversible:true}}:undefined}}}),
 v2x06SurfaceAddress:Object.freeze({locate(planetId,lat,lon,level){return {planetIdentity:planetId,locationIdentity:'loc:'+lat+':'+lon,latMicroDeg:lat,lonMicroDeg:lon,level,unit:[0,1,0]}}}),
 v2x06Geography:Object.freeze({createModel(input){calls.geography++;assert.equal(input.planetIdentity,'planet-a');assert.equal(input.waterAreaPpm,420000);assert.equal(input.tectonicActivityPpm,510000);return {planetIdentity:'planet-a',noSolidSurface:false,sample(){return {reliefCuePpm:250000,surfaceClass:'SOLID',materialFamily:'ROCK_REGOLITH_PRESENTATION',authority:'MODEL_DERIVED_SIMULATION'}}};}}),
 v2x06Hydrology:Object.freeze({createHydrology(){return {sample(){return {}}}}}),
 v2x06HierarchicalTerrain:Object.freeze({createProvider(){return {materializeAdaptive(args){calls.terrain++;calls.lastTerrainArgs={...args};return {status:'READY',planetIdentity:'planet-a',anchorAddress:args.anchorAddress,patches:[],resources:{vertices:256}}},snapshotCache(){return {entries:2,estimatedBytes:8192}},pickSurfaceUnit(){return {planetIdentity:'planet-a',latMicroDeg:13000000,lonMicroDeg:25000000}}}}}),
 v2x06LivingSurfaceRenderer:Object.freeze({VERSION:'v2x06-surface',buildFrame(){return surfaceFrame},renderCanvas2D(){calls.surface++;return {drawCalls:264}}}),
 deep3dSurfaceAdapter:Object.freeze({createRenderScene(frame,options){calls.surface3d++;assert.equal(frame,surfaceFrame);assert(['REGIONAL_SURFACE','LOCAL_SURFACE'].includes(options.stage));assert.deepEqual(Array.from(options.cameraPosition),[1,2,3]);return {frameId:'surface:'+options.stage.toLowerCase(),fingerprint:'surface-test-fingerprint',packets:[{family:'MESH'}]}}}),
 v2x07LocalExperienceProvider:Object.freeze({VERSION:'v2x07-local',createProvider({groundProvider,embodimentProviders}){calls.local++;const ground=groundProvider.sampleGround({xM:2,yM:4});assert.equal(ground.physicalElevationCanonical,false);const pack=embodimentProviders[0].materializeLocal({camera:{headingRad:.2}});assert.equal(pack.objects[0].entityId,'settlement-a');assert.equal(pack.decorations.length,1);return {materialize(){return {authority:'PRESENTATION_ONLY',resources:{objects:2,terrainSamples:221,pickTargets:1,groundSampleCalls:347},terrain:{supported:true},embodiment:{objects:[]},renderFrame:{resources:{primitives:240}}}},render(){return {primitives:240}},pick(){return {hit:true,pickKey:'entity:settlement-a'}}}}}),
 deep3dLocalAdapter:Object.freeze({createRenderScene(frame,options){calls.local3d++;assert.equal(frame.authority,'PRESENTATION_ONLY');assert.equal(options.stage,'HUMAN');return {frameId:'local:loc-a',fingerprint:'local-test-fingerprint',packets:[{family:'MESH'},{family:'MESH'}]}}}),
 v2x12MatterContinuity:Object.freeze({journey(source){calls.matter++;assert.equal(source,exactSource);return {contract:'ofu-v2x12-matter-continuity-journey-1'}}}),
 v2x12MicroscopicPresentation:Object.freeze({PROVIDER_ID:'v2x12.presentation',present(journey,{regime}){calls.micro++;return {providerId:'v2x12.presentation',authority:'PRESENTATION_ONLY',sourceEntityId:'material-source-a',nodes:[{id:'c1',kind:'MATERIAL_COMPONENT',componentId:'rock',position:{x:.3,y:.4}},{id:'c2',kind:'UNRESOLVED_MATERIAL_REMAINDER',position:{x:.7,y:.6}}],nodeCount:2,nodeAvailableCount:2,relations:[],relationCount:0,relationAvailableCount:0,fidelity:{classification:'MODEL_DERIVED'},regime};}}),
 deep3dMatterAdapter:Object.freeze({createRenderScene(presentation,options){calls.matter3d++;assert.equal(presentation.sourceEntityId,'material-source-a');assert.equal(options.stage,'MATERIAL');return {frameId:'matter:material-source-a:material',fingerprint:'matter-test-fingerprint',packets:[{family:'POINT_BATCH'}]}}}),
 waveIVScaleRuntime:Object.freeze({snapshot(){return {anchors:{system:100,orbit:10,approach:2}}}}),
 v1WorldContext:Object.freeze({location(planetIdentity,latMicroDeg,lonMicroDeg){return {planetIdentity,locationIdentity:'picked',latMicroDeg,lonMicroDeg}}}),
 v1LivingProduct:Object.freeze({runtime:Object.freeze({query(id,args){assert.equal(id,'v1.query.material-source');assert.equal(args.objectId,'material-source-a');return exactSource}})})
};
const sandbox={OFU:O,Object,Array,String,Number,Math,Set,Map,Error,TypeError,console};sandbox.globalThis=sandbox;vm.createContext(sandbox);
vm.runInContext(fs.readFileSync(new URL('../../src/rendering/v2x-convergence/living-domain-composition.js',import.meta.url),'utf8'),sandbox,{filename:'living-domain-composition.js'});
assert.equal(sandbox.OFU.v1LivingRenderer.__v2xDomainComposed,true);
assert.equal(sandbox.OFU.v2xLivingDomainComposition.VERSION,'ofu-v2x-living-domain-composition-6');
assert.deepEqual(Array.from(sandbox.OFU.v2xLivingDomainComposition.SYSTEM_STAGES),['SYSTEM','ORBIT','APPROACH']);
const renderer=sandbox.OFU.v1LivingRenderer.create(canvas,glCanvas,{onActivate:n=>calls.activated.push(n),onPoint:p=>calls.points.push(p),onObject:id=>calls.objects.push(id)});
await renderer.render({stage:'SYSTEM',semanticScale:'system',revision:1,continuousDistanceRadii:100,rows:[star,planet],system:{canonicalId:'system-a'}});
let state=renderer.state();
assert.equal(state.domainComposition.owner,'V2X-04');assert.equal(state.domainComposition.provider,'ofu-v2x-04-system-provider-2');assert.equal(state.domainComposition.providerId,'v2x04.system.presentation');assert.equal(state.domainComposition.legacyFallbackUsed,false);assert.equal(state.domainComposition.approachReversible,null);assert.ok(calls.camera>0);
assert.equal(renderer.activateAt(500,320),true);assert.equal(calls.activated[0],planet,'final V2X-04 pick must preserve actual Living node identity');
await renderer.render({stage:'ORBIT',semanticScale:'orbit',revision:2,continuousDistanceRadii:10,rows:[star,planet],system:{canonicalId:'system-a'},body:planet,point});state=renderer.state();assert.equal(state.domainComposition.owner,'V2X-04');assert.equal(state.domainComposition.approachReversible,true);assert.equal(state.domainComposition.legacyFallbackUsed,false);
await renderer.render({stage:'APPROACH',semanticScale:'approach',revision:3,continuousDistanceRadii:2,rows:[star,planet],system:{canonicalId:'system-a'},body:planet,point});state=renderer.state();assert.equal(state.domainComposition.approachReversible,true);assert.equal(calls.system,3,'SYSTEM/ORBIT/APPROACH must all consume final SystemProvider2');
const world={planetIdentity:'planet-a',planetology,civilization:{epoch:0}};
await renderer.render({stage:'REGIONAL_SURFACE',semanticScale:'regional_surface',revision:4,continuousDistanceRadii:.2,point,world,rows:[]});state=renderer.state();assert.equal(state.domainComposition.owner,'V2X-06');assert.equal(state.domainComposition.canonicalElevationClaim,false);assert.equal(state.domainComposition.rivers,3);assert.equal(state.domainComposition.deepPlanetModelDigest,'deep-test-digest');assert.equal(state.domainComposition.primaryBackend,'DEEP3D_WEBGL2_V2X13');assert.equal(state.domainComposition.sceneFingerprint,'surface-test-fingerprint');assert.equal(calls.surface,0,'ready solid surfaces must use Deep3D rather than Canvas pixels');assert.equal(calls.surface3d,2);assert.deepEqual(calls.lastTerrainArgs,{anchorAddress:calls.lastTerrainArgs.anchorAddress,level:5,radius:1,maxPatches:9,detailDemandPpm:460000,resourcePressurePpm:0},'shipping Regional Surface must retain an interactive bounded terrain working set');assert.equal(renderer.activateAt(450,310),true);assert.equal(calls.points[0].planetIdentity,'planet-a');
await renderer.render({stage:'LOCAL_SURFACE',semanticScale:'local_surface',revision:5,continuousDistanceRadii:.02,point,world,rows:[]});state=renderer.state();assert.equal(state.domainComposition.owner,'V2X-06');assert.equal(state.domainComposition.primaryBackend,'DEEP3D_WEBGL2_V2X13');assert.equal(calls.surface3d,4);assert.deepEqual(calls.lastTerrainArgs,{anchorAddress:calls.lastTerrainArgs.anchorAddress,level:7,radius:1,maxPatches:9,detailDemandPpm:720000,resourcePressurePpm:0},'shipping Local Surface must remain on the interactive 17x17 terrain tier');
const local={objects:[{kind:'SETTLEMENT',entityId:'settlement-a',authority:'MODEL_DERIVED_SIMULATION'},{kind:'ORGANISM',entityId:'population-a',authority:'MODEL_DERIVED_SIMULATION'}]};
await renderer.render({stage:'HUMAN',semanticScale:'human',revision:6,continuousDistanceRadii:.00001,point,world,local,rows:[]});state=renderer.state();assert.equal(state.domainComposition.owner,'V2X-07');assert.equal(state.domainComposition.metricGeodesyClaim,false);assert.equal(state.domainComposition.aggregateOrganismsPersistentPersons,false);assert.equal(state.domainComposition.primaryBackend,'DEEP3D_WEBGL2_V2X13');assert.equal(state.domainComposition.sceneFingerprint,'local-test-fingerprint');assert.equal(state.domainComposition.canvasPrimitives,0);assert.equal(calls.local3d,2);assert.equal(renderer.activateAt(100,100),true);assert.equal(calls.objects[0],'settlement-a');
await renderer.render({stage:'MATERIAL',semanticScale:'human',revision:7,continuousDistanceRadii:1,point,selectedObjectId:'material-source-a',world,rows:[]});state=renderer.state();assert.equal(state.domainComposition.owner,'V2X-12');assert.equal(state.domainComposition.sourceEntityId,'material-source-a');assert.equal(state.domainComposition.semanticMagnification,true);assert.equal(state.domainComposition.geometricZoomClaim,false);assert.equal(state.domainComposition.primaryBackend,'DEEP3D_WEBGL2_V2X13');assert.equal(state.domainComposition.sceneFingerprint,'matter-test-fingerprint');assert.equal(calls.matter3d,2);
const staleSurface=renderer.render({stage:'REGIONAL_SURFACE',semanticScale:'regional_surface',revision:8,defer:true,continuousDistanceRadii:.2,point,world,rows:[]});
await Promise.resolve();await renderer.render({stage:'MATERIAL',semanticScale:'human',revision:9,continuousDistanceRadii:1,point,selectedObjectId:'material-source-a',world,rows:[]});releaseDeferred();await staleSurface;state=renderer.state();assert.equal(state.domainComposition.owner,'V2X-12','superseded domain composition must not overwrite the latest scale');assert.equal(state.domainComposition.stage,'MATERIAL');assert.equal(calls.surface3d,4,'superseded surface work must be fenced before the external Deep3D stage boundary');
const unknownSurface=O.v2xLivingDomainComposition.surfaceModelInput({world:{planetIdentity:'planet-a',planetology:{planetIdentity:'planet-a',hydrosphere:{waterAreaPpm:null,iceFractionPpm:null},climate:{aridityPpm:null}}}});
for(const field of ['waterAreaPpm','iceAreaPpm','erosionActivityPpm','aridityPpm','impactActivityPpm'])assert.equal(unknownSurface[field],null);
assert.equal(unknownSurface.tectonicActivityPpm,123000);assert.equal(unknownSurface.volcanicActivityPpm,456000);assert.equal(unknownSurface.deepPlanetContext.provenance.modelId,'v2x05.model.deep-planet');
assert.equal(calls.base,9,'base renderer remains the underlying compatibility/render-resource layer');
console.log(JSON.stringify({schema:'ofu-v2x-living-domain-composition-witness-3',status:'PASS',systemProvider:'ofu-v2x-04-system-provider-2',systemStages:['SYSTEM','ORBIT','APPROACH'],legacyFallbackUsed:false,approachReversible:true,surfaceOwner:'V2X-06',humanOwner:'V2X-07',microOwner:'V2X-12',canonicalMutation:false}));
