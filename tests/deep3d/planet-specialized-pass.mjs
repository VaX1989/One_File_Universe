import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';
const calls={render:0,adapt:0},sandbox={console,Math,OFU:{v1WorldWebGL2:{resourceProfile(){return{mobile:false}},create(){return{render(scene){calls.render++;return{status:'RENDERED',scale:scene.scale}},snapshot(){return{frame:calls.render,contextLost:false}},dispose(){}}}},deep3dPlanetAdapter:{createRenderScene(scene){calls.adapt++;return{fingerprint:'fp:'+scene.sourceId,semanticSelection:{entityId:scene.sourceId}}}}}};sandbox.globalThis=sandbox;vm.createContext(sandbox);vm.runInContext(fs.readFileSync('src/rendering/deep3d/planet-specialized-pass.js','utf8'),sandbox,{filename:'planet-specialized-pass'});assert.equal(sandbox.OFU.v1WorldWebGL2.DEEP3D_PLANET_PASS,'ofu-deep3d-planet-specialized-pass-1');const r=sandbox.OFU.v1WorldWebGL2.create({},{}),w=r.render({scale:'PLANET',sourceId:'planet-a'});assert.equal(calls.adapt,1);assert.equal(calls.render,1);assert.equal(w.backend,'V1_WORLD_WEBGL2_SPECIALIZED');assert.equal(w.sceneFingerprint,'fp:planet-a');const s=r.snapshot();assert.equal(s.deep3d.primary,true);assert.equal(s.deep3d.semanticEntityId,'planet-a');r.render({scale:'SYSTEM',sourceId:'sys'});assert.equal(calls.adapt,1);assert.equal(calls.render,2);console.log(JSON.stringify({status:'PASS',suite:'v2-deep3d-planet-specialized-pass',fingerprint:s.deep3d.sceneFingerprint}));
