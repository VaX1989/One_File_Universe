import fs from 'node:fs';import crypto from 'node:crypto';import {execFileSync} from 'node:child_process';const hash=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');execFileSync(process.execPath,['tools/build-p1.mjs'],{stdio:'ignore'});const a=hash('dist/One_File_Universe.html');const first=fs.readFileSync('dist/One_File_Universe.html');execFileSync(process.execPath,['tools/build-p1.mjs'],{stdio:'ignore'});const b=hash('dist/One_File_Universe.html');if(a!==b||!first.equals(fs.readFileSync('dist/One_File_Universe.html')))throw new Error(`non-reproducible build ${a} != ${b}`);console.log(`P1 reproducible build: PASS ${a}`);
