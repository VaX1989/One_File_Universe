import fs from'node:fs';import vm from'node:vm';import crypto from'node:crypto';import assert from'node:assert/strict';globalThis.OFU={};for(const f of['src/kernel/sha256.js','src/extensions/contracts.js','src/runtime/v2x-01-contracts.js','src/simulation/materialization/cache-key.js','src/runtime/scheduling/adaptive-scheduler.js','src/runtime/resources/resource-ledger.js','src/runtime/workers/worker-executor.js','src/runtime/prefetch/bounded-prefetch.js','src/runtime/lifecycle/lifecycle-coordinator.js','src/simulation/materialization/materialization-runtime.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'),{filename:f});const O=globalThis.OFU,h=x=>crypto.createHash('sha256').update(x).digest('hex');const base={identity:{providerId:'provider.test',entityId:h('e'),representationId:'primary'},modelVersion:'1.0.0',representationVersion:'1.0.0',commitmentDigest:h('c'),historyDigest:h('h'),semanticDigest:h('s')};assert.notEqual(O.v2x01CacheKey.create(base).digest,O.v2x01CacheKey.create({...base,modelVersion:'1.0.1'}).digest);const sch=O.v2x01AdaptiveScheduler.create({budgets:{active:1,queue:4}});let release;const bg=sch.schedule({id:'task.bg',taskClass:'PREFETCH',state:'WARM',run:s=>new Promise((r,j)=>{release=r;s.addEventListener('abort',()=>j(s.reason),{once:true})})});await new Promise(r=>setTimeout(r,0));const urgent=sch.schedule({id:'task.interaction',taskClass:'INTERACTION',state:'IMMEDIATE',run:async()=>1});await bg.promise.catch(()=>{});assert.equal(await urgent.promise,1);assert(sch.snapshot().metrics.preemptionSignals>=1);const rt=O.v2x01MaterializationRuntime.create({budgets:{active:2,queue:8,cacheEntries:2,materializations:2,cpuEstimateBytes:200,gpuEstimateBytes:200,heapBytes:1024,entities:8,operations:500,transferBytes:500,taskBytes:65536}});rt.registerMaterializer({providerId:'provider.test',domain:'test',modelVersion:'1.0.0',representationVersion:'1.0.0',async load(q,s){if(q.semantic.delay)await new Promise((r,j)=>{const t=setTimeout(r,q.semantic.delay);s.addEventListener('abort',()=>{clearTimeout(t);j(s.reason)},{once:true})});return{identity:q.durable.identity,semanticDigest:q.semanticDigest,state:q.targetState,cpuEstimateBytes:20,gpuEstimateBytes:20,entities:1,operations:5,transferBytes:1,value:{v:q.semantic.v}}}});const req=(c,d=0)=>({durable:{identity:{providerId:'provider.test',entityId:h('same'),representationId:'primary'},commitmentDigest:h(c),historyDigest:h('history')},targetState:'HOT',taskClass:'REFINE',semantic:{v:c,delay:d},presentation:{},estimate:{cpuEstimateBytes:30,gpuEstimateBytes:30,entities:1,operations:10,transferBytes:2}});const old=rt.request(req('old',30));await new Promise(r=>setTimeout(r,5));const fresh=rt.request(req('new'));await old.catch(()=>{});assert.equal((await fresh).commitmentDigest,h('new'));const snap=await rt.drain();assert(snap.workingSet.entries<=2);assert(['UNKNOWN','MEASURED_RUNTIME_EVIDENCE'].includes(snap.resources.heap.state));assert.equal(snap.resources.gpu.state,'ESTIMATED');const pf=O.v2x01BoundedPrefetch.create({maxCandidates:2,maxRequests:1});assert.throws(()=>pf.plan({intentSnapshot:{},candidates:[{request:{durable:req('x').durable,targetState:'IMMEDIATE',taskClass:'PREFETCH'},priority:1,distanceHint:1}]}),/AUTHORITY/);console.log(JSON.stringify({status:'PASS',oracle:'V2X01_RUNTIME_TARGETED_INVARIANTS',snapshot:snap}));