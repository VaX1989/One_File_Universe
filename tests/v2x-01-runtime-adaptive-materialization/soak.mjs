import fs from'node:fs';import vm from'node:vm';import crypto from'node:crypto';import assert from'node:assert/strict';globalThis.OFU={};for(const f of['src/kernel/sha256.js','src/extensions/contracts.js','src/runtime/v2x-01-contracts.js','src/simulation/materialization/cache-key.js','src/runtime/scheduling/adaptive-scheduler.js','src/runtime/resources/resource-ledger.js','src/runtime/workers/worker-executor.js','src/simulation/materialization/materialization-runtime.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'),{filename:f});const O=globalThis.OFU,h=x=>crypto.createHash('sha256').update(x).digest('hex'),rt=O.v2x01MaterializationRuntime.create({budgets:{active:3,queue:24,cacheEntries:12,materializations:12,cpuEstimateBytes:2048,gpuEstimateBytes:2048,heapBytes:1048576,entities:64,operations:8192,transferBytes:4096,taskBytes:65536}}),domains=['macro','planet','surface','life','civilization','micro'];for(const d of domains)rt.registerMaterializer({providerId:'provider.'+d,domain:d,modelVersion:'1.0.0',representationVersion:'1.0.0',async load(q){return{identity:q.durable.identity,semanticDigest:q.semanticDigest,state:q.targetState,cpuEstimateBytes:48,gpuEstimateBytes:q.targetState==='WARM'?0:48,entities:1,operations:8,transferBytes:2,value:{d}}}});const req=(d,id,state='HOT')=>({durable:{identity:{providerId:'provider.'+d,entityId:h(id),representationId:'primary'},commitmentDigest:h('c:'+id),historyDigest:h('history')},targetState:state,taskClass:'REFINE',semantic:{d,id},presentation:{},estimate:{cpuEstimateBytes:64,gpuEstimateBytes:state==='WARM'?0:64,entities:1,operations:16,transferBytes:4}}),path=['macro','planet','surface','life','civilization','micro','surface','planet','macro'];for(let j=0;j<120;j++)for(let s=0;s<path.length;s++){const r=await rt.reconcileWorkingSet([req(path[s],j+':'+s,s===3?'WARM':'HOT')]);assert.equal(r[0].status,'fulfilled')}const snap=await rt.drain();assert(snap.workingSet.entries<=12);assert(snap.resources.peak.cpuEstimateBytes<=2048);assert(snap.resources.peak.gpuEstimateBytes<=2048);assert(snap.scheduler.metrics.peakQueued<=24);assert(snap.metrics.evictions>0);console.log(JSON.stringify({status:'PASS',oracle:'V2X01_LONG_RESOURCE_PLATEAU',journeys:120,steps:1080,snapshot:snap}));