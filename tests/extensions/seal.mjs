import assert from 'node:assert/strict';import {createHash} from 'node:crypto';import {validatePXEvidence} from '../../tools/extensions/seal.mjs';
// These synthetic records falsify the seal; they are never runtime evidence.
const digest='a'.repeat(64),source='b'.repeat(40),bands=['galaxy','stellar_neighborhood','system','orbit','approach','global_surface','regional_surface','local_surface','human'],composition={},build={sourceCommit:source,artifactSha256:digest,additiveComponents:composition,componentCompositionSha256:createHash('sha256').update(JSON.stringify(composition)).digest('hex'),px:{version:'ofu-px-product-1',scope:'FULL_WAVE_IV_PRODUCT',registryDigest:digest,regimes:bands.map(id=>({id})),registryManifest:{providers:[{id:'px.domain',kind:'domain',mandatory:true},{id:'px.scene',kind:'scene',mandatory:true}]}}};
const rows=['darwin/arm64/webkit','linux/x64/chromium','linux/x64/firefox','linux/x64/webkit','win32/x64/chromium'].map(tuple=>{const [platform,arch,browser]=tuple.split('/');return{schema:'ofu-px-browser-evidence-1',status:'PASS',sourceSha:source,artifactSha256:digest,componentCompositionSha256:build.componentCompositionSha256,platform,arch,browser,capability:{webgl2:true},directFile:true,offline:true,unexpectedNetworkRequests:0,pageErrors:0,physicalAndroid:'NOT_VERIFIED',physicalIOS:'NOT_VERIFIED',registry:{sealed:true,bindingsSealed:true,manifestDigest:digest,entries:2,bound:['px.domain','px.scene']},providerChecks:['px.domain'],canonicalBefore:digest,canonicalAfter:digest,p4Before:digest,p4After:digest,metrics:{renderCalls:100,reconciliations:100,refinements:50,projections:50},workingSet:{cacheEntries:1,witnessCount:32,resources:{entries:2,totalDecodedBytes:1000}},journeys:Array.from({length:3},()=>bands.map(scale=>({scale,witness:digest,backend:['regional_surface','local_surface','human'].includes(scale)?'webgl2-local-surface':['orbit','approach','global_surface'].includes(scale)?'webgl2':'canvas2d'}))).flat()};});
validatePXEvidence(rows,build,source);let cases=1;
for(const mutate of [x=>x.pop(),x=>x[0].sourceSha='c'.repeat(40),x=>x[0].artifactSha256='f'.repeat(64),x=>x[0].componentCompositionSha256='f'.repeat(64),x=>x[0].registry.manifestDigest='f'.repeat(64),x=>x[0].registry.bound=[],x=>x[0].capability.webgl2=false,x=>x[0].directFile=false,x=>x[0].physicalIOS='PASS',x=>x[0].metrics.refinements=0,x=>x[0].metrics.projections=0,x=>x[0].metrics.renderCalls=0,x=>x[0].providerChecks=[],x=>x[0].canonicalAfter='f'.repeat(64),x=>x[0].workingSet.cacheEntries=17,x=>x[0].journeys=x[0].journeys.filter(j=>j.scale!=='human'),x=>x[1]={...x[0]}]){const changed=structuredClone(rows);mutate(changed);assert.throws(()=>validatePXEvidence(changed,build,source));cases++;}
console.log(JSON.stringify({status:'PASS',suite:'px-seal',cases,syntheticOnly:true}));
