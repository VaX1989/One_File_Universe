import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
globalThis.OFU={planetRenderCore:{}};vm.runInThisContext(fs.readFileSync('src/rendering/planet-surface.js','utf8'));const S=OFU.planetSurface;
const provider={planetId:'fixture-giant',presentationBounds:{maxRadiusM:70000000},physical:{physical:{meanRadiusM:69900000}}};for(const bodyClass of ['GAS_GIANT','ICE_GIANT']){const c=S.surfaceCapability({bodyClass,provider,planetId:'fixture-'+bodyClass.toLowerCase()});assert.equal(c.available,false);assert.equal(c.reason,'NO_SUPPORTED_SOLID_LOCAL_SURFACE');assert.equal(c.claims.physicalTerrainElevationClaim,false);const h=S.handoffFromGlobe({bodyClass,provider,planetId:'fixture-'+bodyClass.toLowerCase()},{viewDirection:[0,0,1],radiusM:69900000});assert.equal(h.ready,false);assert.equal(h.reason,'NO_SUPPORTED_SOLID_LOCAL_SURFACE')}
const unknown=S.surfaceCapability({bodyClass:'UNKNOWN',provider});assert.equal(unknown.available,false);assert.equal(unknown.reason,'CANONICAL_SURFACE_MODEL_UNAVAILABLE');const terrestrial=S.surfaceCapability({bodyClass:'TERRESTRIAL',provider:{...provider,planetId:'fixture-terrestrial'}});assert.equal(terrestrial.available,true);console.log(JSON.stringify({status:'PASS',gasGiant:'FAIL_CLOSED',iceGiant:'FAIL_CLOSED',unknown:'FAIL_CLOSED',terrestrial:'AVAILABLE_WHEN_BOUNDED',claims:S.CLAIMS}));
