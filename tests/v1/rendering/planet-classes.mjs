import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';globalThis.OFU={};for(const f of ['src/rendering/v1/presentation-core.js','src/rendering/v1/planet-presentation.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'),{filename:f});const P=OFU.v1PlanetPresentation,A={class:'MODEL_DERIVED_SIMULATION'};const base={atmosphere:{inventoryUnits:0,authority:A},interior:{heatIndexPpm:200000},surfaceProcesses:{impactRetentionPpm:600000,erosionPotentialPpm:100000,tectonicActivityPpm:150000},climate:{surfaceTemperatureMilliK:250000,authority:A},authority:A};function world(id,bulk,water,ice,atm=0){return{planetIdentity:id,planetology:{...base,planetIdentity:id,bulkPriorClass:bulk,atmosphere:{inventoryUnits:atm,authority:A},hydrosphere:{waterAreaPpm:water,iceFractionPpm:ice,canonicalOceanClaim:false,authority:A}}}}const dry=P.planet({world:world('dry','TERRESTRIAL',0,0)}),ocean=P.planet({world:world('ocean','TERRESTRIAL',760000,40000,50000)}),icy=P.planet({world:world('icy','VOLATILE_RICH',0,840000,12000)});assert.equal(dry.layers.find(x=>x.kind==='HYDROSPHERE_ENCODING').enabled,false);assert.equal(ocean.objects[0].visual.waterAreaPpm,760000);assert.ok(ocean.objects[0].visual.atmosphereStrength>0);assert.equal(icy.objects[0].visual.iceAreaPpm,840000);assert.notDeepEqual(dry.objects[0].visual.primaryColor,icy.objects[0].visual.primaryColor);for(const s of [dry,ocean,icy]){assert.equal(s.authority,'PRESENTATION_ONLY');assert.equal(s.objects[0].claims.surfacePatternIsCanonicalMap,false);assert.equal(s.layers.some(x=>x.kind==='CLOUD_COVER'),false)}console.log('v1 planet presentation classes: PASS');
