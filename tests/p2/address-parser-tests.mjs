import fs from'node:fs';import vm from'node:vm';import assert from'node:assert/strict';globalThis.OFU={};for(const f of['src/kernel/sha256.js','src/kernel/p2-canonical.js','src/kernel/p2-address-parser.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'));const P=OFU.p2,a=P.address([{kind:'namespace',value:'local'},{kind:'i64',value:-1n},{kind:'u64',value:18446744073709551615n},{kind:'bytes',value:Uint8Array.of(0,1,255)}]),r=P.parseAddress(a);assert.equal(r[0].value,'local');assert.equal(r[1].value,-1n);assert.equal(r[2].value,18446744073709551615n);assert.deepEqual([...r[3].value],[0,1,255]);for(const bad of[Uint8Array.of(),Uint8Array.from(a.slice(0,-1)),Uint8Array.from([...a,0]),Uint8Array.from([79,70,85,65,2,1,1,1,120]),Uint8Array.from([79,70,85,65,1,1,255])])assert.throws(()=>P.parseAddress(bad));let unknown=Uint8Array.from(a);unknown[6]=255;assert.throws(()=>P.parseAddress(unknown),/unknown segment tag/);console.log('P2 address parser: PASS');
