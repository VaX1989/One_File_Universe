import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';globalThis.OFU={};for(const f of ['src/kernel/sha256.js','src/kernel/canonical.js','src/persistence/save.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'),{filename:f});const O=OFU,seed='0'.repeat(64),manifest='1'.repeat(64),chunk='x'.repeat(O.save.LIMITS.stringBytes),events=Array.from({length:70},()=>({type:'x',data:chunk}));assert.throws(()=>O.save.exportPortable({masterSeed256:seed,manifestHash:manifest,events}),/save exceeds limit/,'export must not create a save rejected solely by the importer final-size limit');const normal=O.save.exportPortable({masterSeed256:seed,manifestHash:manifest,events:[{type:'x',data:'ok'}]});assert.deepEqual(O.save.importPortable(normal),O.save.payload({masterSeed256:seed,manifestHash:manifest,events:[{type:'x',data:'ok'}]}));console.log('P1 portable save size symmetry: PASS');
