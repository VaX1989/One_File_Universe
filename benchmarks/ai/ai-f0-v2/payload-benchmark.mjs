import {spawnSync} from 'node:child_process';import {performance} from 'node:perf_hooks';import crypto from 'node:crypto';
const SELF=new URL(import.meta.url).pathname;if(process.argv[2]==='--one'){const n=Number(process.argv[3]);if(!Number.isSafeInteger(n)||n<=0)process.exit(2);global.gc?.();const before=process.memoryUsage();const raw=Buffer.allocUnsafe(n);for(let off=0;off<n;off+=65536)crypto.createHash('sha256').update(String(off)).digest().copy(raw,off,0,Math.min(32,n-off));const t0=performance.now(),b64=raw.toString('base64'),t1=performance.now(),decoded=Buffer.from(b64,'base64'),t2=performance.now(),after=process.memoryUsage();console.log(JSON.stringify({payloadBytes:n,base64Bytes:Buffer.byteLength(b64),encodeMs:t1-t0,decodeMs:t2-t1,rssDelta:after.rss-before.rss,externalDelta:after.external-before.external,arrayBuffersDelta:after.arrayBuffers-before.arrayBuffers,checksum:crypto.createHash('sha256').update(decoded).digest('hex')}));process.exit(0);}const sizes=[16,64,112].map(m=>m*1024*1024),results=[];for(const n of sizes){const r=spawnSync(process.execPath,['--expose-gc',SELF,'--one',String(n)],{encoding:'utf8',maxBuffer:1024*1024});if(r.status!==0)throw new Error(r.stderr||`child ${r.status}`);results.push(JSON.parse(r.stdout));}console.log(JSON.stringify({schema:'ofu-ai-f0-payload-proxy-2',runtime:process.version,platform:`${process.platform}-${process.arch}`,classification:'MEASURED_LOCAL_SYNTHETIC_PROXY_NOT_MODEL_INFERENCE',results},null,2));
