(function(root){
'use strict';const O=root.OFU=root.OFU||{};
function createSource(kernelSources,manifestHash){return `${kernelSources}\nself.OFU.runtimeManifestHash=${JSON.stringify(manifestHash)};\nself.onmessage=(e)=>{const {seed,jobs}=e.data;const rows=jobs.map(j=>({id:j.id,fact:self.OFU.micro.sample(seed,BigInt(j.x),BigInt(j.y),BigInt(j.z))}));const bytes=new TextEncoder().encode(JSON.stringify(rows));self.postMessage({rows,buffer:bytes.buffer},[bytes.buffer]);};`;}
async function run(seed,jobs,count,kernelSources,manifestHash){if(typeof Worker!=='function'||typeof URL==='undefined'||typeof Blob!=='function')throw new Error('workers unavailable');const n=Math.max(1,Math.min(count,jobs.length||1)),buckets=Array.from({length:n},()=>[]);jobs.forEach((j,i)=>buckets[i%n].push(j));const url=URL.createObjectURL(new Blob([createSource(kernelSources,manifestHash)],{type:'text/javascript'}));try{const parts=await Promise.all(buckets.map(bucket=>new Promise((resolve,reject)=>{const w=new Worker(url);w.onerror=e=>{w.terminate();reject(new Error(e.message||'worker error'));};w.onmessage=e=>{w.terminate();resolve({rows:e.data.rows,transferBytes:e.data.buffer.byteLength});};w.postMessage({seed,jobs:bucket});})));return {rows:parts.flatMap(p=>p.rows).sort((a,b)=>a.id-b.id),transferBytes:parts.reduce((s,p)=>s+p.transferBytes,0)};}finally{URL.revokeObjectURL(url);}}
O.workerHarness={createSource,run};
})(typeof globalThis!=='undefined'?globalThis:this);
