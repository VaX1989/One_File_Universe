(function(root){
'use strict';
if(typeof document==='undefined')return;
const O=root.OFU=root.OFU||{},VERSION='ofu-v2-cinematic-macro-director-7',AUTHORITY='PRESENTATION_ONLY',MAX_BOOT_ATTEMPTS=240;
const macroStages=new Set(['UNIVERSE','GALAXY','REGION','NEIGHBORHOOD','SYSTEM']);
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const short=x=>String(x||'').slice(0,8);
let stage=null,canvas=null,g=null,width=1,height=1,dpr=1,frames=0,lastStage=null,lastHero=null,active=false,scheduled=false,rafId=0,unsubscribe=null,lastPresentationKey=null,invalidations=0,coalescedInvalidations=0,redrawSkips=0,presentedRevision=-1,bootTimer=0,bootAttempts=0;
function hash(text){let h=2166136261;for(const ch of String(text||'')){h^=ch.charCodeAt(0);h=Math.imul(h,16777619);}return h>>>0;}
function dependencies(){return O.v1LivingProduct&&O.v1x02SpatialUniverse&&O.v1x04SystemProvider&&O.v2x02LivingCameraComposition&&O.v1LivingRenderer;}
function identityOf(entity,fallback=null){for(const key of ['canonicalId','entityId','id'])if(typeof entity?.[key]==='string'&&entity[key])return entity[key];if(typeof fallback==='string'&&fallback)return fallback;if(entity?.canonicalKey&&typeof entity.canonicalKey==='object')return 'canonicalKey:'+JSON.stringify(entity.canonicalKey,Object.keys(entity.canonicalKey).sort(),0);return null;}
function jsonToken(value){try{return JSON.stringify(value,(_,v)=>typeof v==='bigint'?String(v):v)||'';}catch{return '';}}
function surfacePlan(){const box=stage.getBoundingClientRect(),cssWidth=Math.max(1,box.width),cssHeight=Math.max(1,box.height),cap=Math.sqrt(1800000/(cssWidth*cssHeight)),ratio=Math.max(.5,Math.min(1.5,root.devicePixelRatio||1,cap));return Object.freeze({cssWidth,cssHeight,dpr:ratio,pixelWidth:Math.max(1,Math.floor(cssWidth*ratio)),pixelHeight:Math.max(1,Math.floor(cssHeight*ratio))});}
function fit(plan){width=plan.cssWidth;height=plan.cssHeight;dpr=plan.dpr;if(canvas.width!==plan.pixelWidth||canvas.height!==plan.pixelHeight){canvas.width=plan.pixelWidth;canvas.height=plan.pixelHeight;}g.setTransform(dpr,0,0,dpr,0,0);}
function radial(x,y,r,inner,outer='rgba(0,0,0,0)'){const q=g.createRadialGradient(x,y,0,x,y,r);q.addColorStop(0,inner);q.addColorStop(1,outer);g.fillStyle=q;g.fillRect(x-r,y-r,r*2,r*2);}
function backdrop(s){const centerX=s.stage==='GALAXY'?width*.70:width*.60,centerY=height*.48,bg=g.createRadialGradient(centerX,centerY,0,centerX,centerY,Math.max(width,height)*.82);bg.addColorStop(0,s.stage==='SYSTEM'?'#10202b':'#0b1a26');bg.addColorStop(.5,'#06111a');bg.addColorStop(1,'#01060b');g.fillStyle=bg;g.fillRect(0,0,width,height);const count=s.stage==='UNIVERSE'?24:20,seed=hash([s.stage,s.node?.entityId,s.galaxy?.entityId,s.system?.entityId].join('|'));for(let i=0;i<count;i++){let n=(seed+Math.imul(i+1,2654435761))>>>0;n^=n<<13;n^=n>>>17;n^=n<<5;const x=(n>>>0)/4294967296*width;n^=n<<13;n^=n>>>17;n^=n<<5;const y=(n>>>0)/4294967296*height;n^=n<<13;n^=n>>>17;n^=n<<5;const b=(n>>>0)/4294967296;g.fillStyle='rgba(218,232,238,'+(.035+b*.12).toFixed(3)+')';g.beginPath();g.arc(x,y,.4+b*.8,0,Math.PI*2);g.fill();}}
function galaxy(x,y,r,node,{hero=false,ghost=false}={}){const profile=node?.metadata?.modelProfile||{},morph=String(profile.morphology||'UNKNOWN'),rot=(hash(identityOf(node,'galaxy'))%628)/100;g.save();g.translate(x,y);g.rotate(rot);g.scale(1,.56);radial(0,0,r*2.9,ghost?'rgba(84,129,160,.045)':hero?'rgba(100,154,189,.19)':'rgba(94,141,171,.08)');g.globalCompositeOperation='screen';const disk=g.createRadialGradient(0,0,r*.05,0,0,r*1.2);disk.addColorStop(0,ghost?'rgba(245,215,174,.10)':'rgba(255,225,177,.88)');disk.addColorStop(.17,ghost?'rgba(160,187,202,.06)':'rgba(185,207,216,.34)');disk.addColorStop(.74,ghost?'rgba(82,123,151,.018)':'rgba(83,128,159,.11)');disk.addColorStop(1,'rgba(0,0,0,0)');g.fillStyle=disk;g.beginPath();g.ellipse(0,0,r*1.18,r*.7,0,0,Math.PI*2);g.fill();if(morph.includes('SPIRAL')||morph.includes('DISK'))for(let arm=0;arm<3;arm++){g.beginPath();for(let j=0;j<=58;j++){const t=j/58,a=arm*Math.PI*2/3+t*5.5,rr=(.06+t*.98)*r,xx=Math.cos(a)*rr,yy=Math.sin(a)*rr;j?g.lineTo(xx,yy):g.moveTo(xx,yy);}g.strokeStyle=ghost?'rgba(147,180,196,.055)':hero?'rgba(190,218,226,.44)':'rgba(155,190,204,.18)';g.lineWidth=hero?3.5:1.8;g.stroke();}g.globalCompositeOperation='source-over';if(!ghost){g.strokeStyle='rgba(20,17,17,.34)';g.lineWidth=Math.max(2,r*.05);g.beginPath();g.ellipse(0,r*.04,r*.93,r*.105,-.13,0,Math.PI*2);g.stroke();radial(-r*.04,-r*.02,r*.40,'rgba(255,228,184,.84)');}g.restore();}
function text(value,x,y,{size=11,alpha=1,bold=false}={}){g.save();g.globalAlpha=alpha;g.font=(bold?'600 ':'500 ')+size+'px system-ui,sans-serif';g.textAlign='center';g.textBaseline='middle';g.fillStyle='#d7e2e4';g.shadowColor='rgba(0,0,0,.92)';g.shadowBlur=11;g.fillText(value,x,y);g.restore();}
function labelSlots(items,max){const accepted=[];for(const item of items){if(accepted.length>=max)break;const w=Math.min(190,Math.max(72,item.label.length*6.2)),rect={l:item.x-w/2,r:item.x+w/2,t:item.y+item.r+12,b:item.y+item.r+34};if(rect.l<20||rect.r>width-20||rect.t<118||rect.b>height-82)continue;if(accepted.some(a=>!(rect.r<a.rect.l||rect.l>a.rect.r||rect.b<a.rect.t||rect.t>a.rect.b)))continue;accepted.push({...item,rect});}return accepted;}
function cameraAngles(){const authority=O.v1LivingProduct.renderer.cameraAuthority?.snapshot?.();return authority?O.v2x02LivingCameraComposition.orientationAngles(authority):{yaw:0,pitch:0};}
function presentationKey(s,plan,angles){const scope=[identityOf(s.node),identityOf(s.galaxy),identityOf(s.region),identityOf(s.neighborhood),identityOf(s.system),identityOf(s.body)].join('>');const rows=hash((s.rows||[]).map(row=>[row.kind,identityOf(row),row.metadata?.modelProfile?.morphology||'',jsonToken(row.metadata?.facts||null)].join(':')).join('|'));return [s.stage,s.semanticScale,Number(s.continuousDistanceRadii).toPrecision(12),angles.yaw.toFixed(9),angles.pitch.toFixed(9),plan.cssWidth.toFixed(2),plan.cssHeight.toFixed(2),plan.pixelWidth,plan.pixelHeight,scope,rows].join('|');}
function cameraFor(s,scale,{system=false,angles=null}={}){angles=angles||cameraAngles();const anchors=O.waveIVScaleRuntime?.snapshot?.().anchors||{},anchor=anchors[s.semanticScale]||null,distance=Number(s.continuousDistanceRadii),ratio=anchor&&Number.isFinite(distance)&&distance>0?clamp(distance/anchor,.45,2.4):1,d=scale*(system?2.8:2.05)*ratio,cp=Math.cos(angles.pitch),position=[Math.sin(angles.yaw)*cp*d,Math.sin(angles.pitch)*d,Math.cos(angles.yaw)*cp*d];const norm=v=>{const n=Math.hypot(...v)||1;return v.map(x=>x/n)},cross=(a,b)=>[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]],forward=norm(position.map(x=>-x)),right=norm(cross(forward,[0,1,0])),up=norm(cross(right,forward));return O.v1LivingRenderer.adaptPresentationCamera({position,origin:position,target:[0,0,0],right,up,forward,focalLength:1.18,near:scale*.01,fovYRadians:Math.PI/3,aspect:width/height,far:scale*20});}
function spatial(s,angles){const context=s.stage==='UNIVERSE'?'UNIVERSE':s.stage==='GALAXY'?'GALAXY':s.stage==='REGION'?'REGION':'NEIGHBORHOOD',scopeEntity=s.stage==='UNIVERSE'?s.node:s.stage==='GALAXY'?s.galaxy:s.stage==='REGION'?s.region:s.neighborhood,scopeId=identityOf(scopeEntity,scopeEntity?.entityId),sp=O.v1x02SpatialUniverse,profile=sp.profile({context}),rep=sp.representation({context,scopeId,entities:s.rows,cameraFrame:cameraFor(s,profile.scaleUnits,{angles}).spatialFrame,limit:64}),visible=[];for(const o of rep.objects){if(!o.view?.visible)continue;const objectIdentity=o.canonicalId||o.entityId||o.identity,node=s.rows.find(n=>String(n.canonicalId||n.entityId||identityOf(n))===String(objectIdentity));if(!node)continue;const rawX=width*.5+o.view.x*Math.min(width,height)*.46,rawY=height*.5-o.view.y*Math.min(width,height)*.46,depth=Number(o.view.depth)||profile.scaleUnits,depthScale=clamp(profile.scaleUnits/depth,.48,1.45);visible.push({node,identity:identityOf(node,String(objectIdentity)),rawX,rawY,depth,depthScale});}visible.sort((a,b)=>a.depth-b.depth||String(a.identity).localeCompare(String(b.identity)));const projectedHero=visible[0]||null;if(s.stage==='UNIVERSE'){const fallback=s.rows.find(n=>n.kind==='galaxy')||s.rows[0]||null,heroNode=projectedHero?.node||fallback,heroIdentity=projectedHero?.identity||identityOf(fallback);if(heroNode&&heroIdentity){const px=projectedHero?.rawX??width*.5,py=projectedHero?.rawY??height*.5,heroX=width*.66+clamp((px-width*.5)*.20,-width*.07,width*.07),heroY=height*.50+clamp((py-height*.5)*.20,-height*.07,height*.07),heroR=Math.min(width,height)*.27;for(const item of visible.slice(1).reverse()){const r=clamp(22*item.depthScale,10,28);if(item.node.kind==='galaxy')galaxy(item.rawX,item.rawY,r,item.node,{ghost:true});else{radial(item.rawX,item.rawY,r*2.5,'rgba(105,165,190,.06)');g.fillStyle='rgba(142,190,207,.46)';g.beginPath();g.arc(item.rawX,item.rawY,Math.max(2,r*.16),0,Math.PI*2);g.fill();}}galaxy(heroX,heroY,heroR,heroNode,{hero:true});text('Galaxy '+short(heroIdentity),heroX,Math.min(height-96,heroY+heroR*.73),{size:12,alpha:.92,bold:true});lastHero=heroIdentity;}return;}lastHero=identityOf(scopeEntity,scopeId)||projectedHero?.identity||null;if(s.stage==='GALAXY')galaxy(width*.70,height*.50,Math.min(width,height)*.42,s.galaxy,{hero:true,ghost:true});const labels=[];for(const item of visible.slice().reverse()){const {node,rawX:x,rawY:y,depth,depthScale,identity}=item,isStar=node.kind==='star',isGalaxy=node.kind==='galaxy',r=isGalaxy?clamp(24*depthScale,11,34):clamp((isStar?10:7)*depthScale,3,14);if(isGalaxy)galaxy(x,y,r,node,{ghost:true});else{radial(x,y,r*(isStar?3.5:2.6),isStar?'rgba(255,198,111,.12)':'rgba(102,164,189,.075)');g.fillStyle=isStar?'rgba(244,211,155,.82)':'rgba(139,188,204,.72)';g.beginPath();g.arc(x,y,r,0,Math.PI*2);g.fill();}const label=(isGalaxy?'Galaxy':node.kind==='galactic_region'?'Region':node.kind==='system'?'System':isStar?'Star':'World')+' '+short(identity);labels.push({x,y,r,label,hero:false,depth});}for(const item of labelSlots(labels.sort((a,b)=>a.depth-b.depth),s.stage==='GALAXY'?2:3))text(item.label,item.x,item.rect.t+10,{size:10,alpha:.58});}
function system(s,angles){const provider=O.v1x04SystemProvider,sys={id:s.system.canonicalId,facts:s.system.metadata?.facts||{}},stars=s.rows.filter(n=>n.kind==='star').map(n=>({id:n.canonicalId||n.entityId,facts:n.metadata?.facts||{}})),planets=s.rows.filter(n=>n.kind==='planet').map(n=>({id:n.canonicalId||n.entityId,facts:n.metadata?.facts||{}})),rendered=provider.render({system:sys,stars,planets},{camera:cameraFor(s,28,{system:true,angles}).systemFrame,viewport:{width,height}}),labels=[];lastHero=identityOf(s.system,s.system?.canonicalId||s.system?.entityId);for(const orbit of rendered.projectedOrbits){if(orbit.points.length<2)continue;g.beginPath();orbit.points.forEach((p,i)=>i?g.lineTo(p.x,p.y):g.moveTo(p.x,p.y));g.strokeStyle='rgba(151,177,188,.12)';g.lineWidth=1;g.stroke();}for(const h of rendered.hitTargets){if(!h.visible)continue;const node=s.rows.find(n=>String(n.canonicalId||n.entityId)===String(h.canonicalEntityId));if(!node)continue;const star=node.kind==='star',r=star?Math.min(width,height)*.105:8;radial(h.x,h.y,star?r*3:30,star?'rgba(255,190,103,.25)':'rgba(100,157,184,.12)');g.fillStyle=star?'#ffe0a4':'#8ab7c9';g.beginPath();g.arc(h.x,h.y,r,0,Math.PI*2);g.fill();if(star){radial(h.x-r*.18,h.y-r*.22,r*.7,'rgba(255,249,220,.72)');lastHero=identityOf(node,String(h.canonicalEntityId));}labels.push({x:h.x,y:h.y,r,label:(star?'Star ':'World ')+short(h.canonicalEntityId),hero:star,depth:star?0:1});}for(const item of labelSlots(labels.sort((a,b)=>Number(b.hero)-Number(a.hero)),4))text(item.label,item.x,item.rect.t+10,{size:item.hero?12:10,alpha:item.hero?1:.62,bold:item.hero});}
function draw(){if(!g||!dependencies())return;const s=O.v1LivingProduct.runtime.snapshot(),nextActive=macroStages.has(s.stage),plan=surfacePlan(),angles=nextActive?cameraAngles():{yaw:0,pitch:0},key=presentationKey(s,plan,angles);active=nextActive;canvas.hidden=!active;presentedRevision=s.revision;if(key===lastPresentationKey){redrawSkips++;lastStage=s.stage;return;}lastPresentationKey=key;if(!active){g.clearRect(0,0,width,height);lastStage=s.stage;lastHero=null;return;}fit(plan);backdrop(s);if(s.stage==='SYSTEM')system(s,angles);else spatial(s,angles);lastStage=s.stage;frames++;}
const pointerMove=event=>{if(event.buttons)schedule();};
function schedule(){invalidations++;if(scheduled){coalescedInvalidations++;return;}scheduled=true;rafId=root.requestAnimationFrame(()=>{rafId=0;scheduled=false;draw();});}
function scheduleBoot(){if(stage||bootTimer||bootAttempts>=MAX_BOOT_ATTEMPTS)return;bootTimer=root.setTimeout(()=>{bootTimer=0;boot();},25);}
function boot(){if(stage)return true;bootAttempts++;const candidate=document.getElementById('living-stage');if(!dependencies()||!candidate){scheduleBoot();return false;}const next=document.createElement('canvas');next.id='v2-cinematic-macro';next.hidden=true;next.setAttribute('aria-hidden','true');next.dataset.authority=AUTHORITY;candidate.append(next);const nextContext=next.getContext('2d',{alpha:false});if(!nextContext){next.remove();scheduleBoot();return false;}stage=candidate;canvas=next;g=nextContext;unsubscribe=O.v1LivingProduct.runtime.onChange(schedule);stage.addEventListener('pointermove',pointerMove,{passive:true,capture:true});stage.addEventListener('wheel',schedule,{passive:true,capture:true});root.addEventListener('resize',schedule,{passive:true});schedule();return true;}
function dispose(){root.clearTimeout(bootTimer);bootTimer=0;if(rafId)root.cancelAnimationFrame?.(rafId);rafId=0;scheduled=false;unsubscribe?.();unsubscribe=null;stage?.removeEventListener('pointermove',pointerMove,{capture:true});stage?.removeEventListener('wheel',schedule,{capture:true});root.removeEventListener('resize',schedule);canvas?.remove();stage=canvas=g=null;active=false;}
function snapshot(){return Object.freeze({version:VERSION,authority:AUTHORITY,active,stage:lastStage,frames,heroIdentity:lastHero,presentedRevision,scheduled,bootAttempts,maxBootAttempts:MAX_BOOT_ATTEMPTS,maxSurfacePixels:1800000,invalidations,coalescedInvalidations,redrawSkips,continuousAnimation:false,semanticMutation:false,cameraAuthority:false,navigationAuthority:false,networkResources:0,normalPrimaryPath:false,layerRole:'PRESENTATION_UNDERLAY_SUPERSEDED_BY_DEEP3D_PRIMARY'});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
O.v2CinematicMacroDirector=Object.freeze({VERSION,AUTHORITY,macroStages:Object.freeze([...macroStages]),snapshot,dispose});
})(globalThis);
