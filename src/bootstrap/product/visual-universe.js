(function(root){
'use strict';
const O=root.OFU=root.OFU||{},U=O.v09UniversePresentation;
if(!U||typeof document==='undefined')return;
const AUTHORITY='PRESENTATION_ONLY',VERSION='ofu-visual-universe-controller-2',STAGES=Object.freeze(['system','orbit','approach','close','surface','global_surface','regional_surface','local_surface','human']);
const SYSTEM_FIELDS=Object.freeze(['galaxyX','galaxyY','galaxyZ','sectorX','sectorY','sectorZ','siteX','siteY','siteZ']);
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),rgba=(rgb,a=1)=>`rgba(${rgb.map(v=>Math.round(clamp(v,0,1)*255)).join(',')},${clamp(a,0,1)})`;
const state={version:VERSION,authority:AUTHORITY,scene:null,stage:'system',selectedOrbitSlot:0,transition:null,canvas:null,ctx:null,viewport:null,reducedMotion:false,lastSystemKey:null,lastProductStage:null,lastSelectedSlot:null,frames:0,lastRenderAt:0,primaryPlanetOwner:'WEBGL_PLANET_RENDERER'};
function freezeKey(key){return Object.freeze(Object.fromEntries(SYSTEM_FIELDS.map(name=>[name,BigInt(key[name])]))) }
function keyString(key){return SYSTEM_FIELDS.map(name=>String(key[name])).join('/')}
function ensureCanvas(){if(state.canvas?.isConnected)return state.canvas;const planet=document.getElementById('planet-view'),frame=planet?.parentElement;if(!planet||!frame)return null;const canvas=document.createElement('canvas');canvas.id='visual-universe-overlay';canvas.setAttribute('aria-hidden','true');canvas.dataset.authority=AUTHORITY;canvas.dataset.primaryPlanet='false';planet.insertAdjacentElement('afterend',canvas);state.canvas=canvas;state.ctx=canvas.getContext('2d',{alpha:true});return canvas}
function resize(){const canvas=ensureCanvas();if(!canvas||!state.ctx)return false;const frame=canvas.parentElement,r=frame.getBoundingClientRect(),dpr=Math.max(1,Math.min(2,Number(root.devicePixelRatio)||1)),w=Math.max(2,Math.round(r.width*dpr)),h=Math.max(2,Math.round(r.height*dpr));state.viewport=Object.freeze({cssWidth:r.width,cssHeight:r.height,devicePixelRatio:dpr,backingWidth:w,backingHeight:h,geometryDprIndependent:true});if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}state.ctx.setTransform(dpr,0,0,dpr,0,0);return true}
function clear(){if(state.ctx&&state.viewport)state.ctx.clearRect(0,0,state.viewport.cssWidth,state.viewport.cssHeight)}
function drawDecorativeField(g,scene,w,h,{excludeCenter=0,opacity=1}={}){for(const p of scene?.decorativeStarField?.points||[]){const x=p.x*w,y=p.y*h;if(excludeCenter>0&&Math.hypot((x-w/2)/Math.max(1,w/2),(y-h/2)/Math.max(1,h/2))<excludeCenter)continue;g.globalAlpha=p.opacity*opacity;g.fillStyle='#c7d0d8';g.beginPath();g.arc(x,y,p.sizePx,0,Math.PI*2);g.fill()}g.globalAlpha=1}
function bodyPoint(body,cx,cy,maxOrbit){const r=maxOrbit*(.18+.82*body.normalizedOrbit),a=body.phaseAngleRad;return{x:cx+Math.cos(a)*r,y:cy+Math.sin(a)*r*.55,radius:r}}
function drawStar(g,star,x,y,scale=1){const d=star.descriptor,r=d.displayRadiusPx*scale;if(d.displayMode==='NON_EMISSIVE_REMNANT_MARKER'){g.strokeStyle=rgba(d.displayRgb,.72);g.beginPath();g.arc(x,y,Math.max(4,r*.52),0,Math.PI*2);g.stroke();return}const grad=g.createRadialGradient(x,y,0,x,y,r*2.25);grad.addColorStop(0,rgba(d.displayRgb,.98));grad.addColorStop(.34,rgba(d.displayRgb,.62*d.displayIntensity));grad.addColorStop(1,rgba(d.displayRgb,0));g.fillStyle=grad;g.beginPath();g.arc(x,y,r*2.25,0,Math.PI*2);g.fill();g.fillStyle=rgba(d.displayRgb,.92);g.beginPath();g.arc(x,y,Math.max(2.5,r*.58),0,Math.PI*2);g.fill()}
function drawSystem(g,scene,w,h,alpha=1,transitionProgress=null){if(!scene||scene.status!=='READY')return;g.save();g.globalAlpha=alpha;const bg=g.createRadialGradient(w*.48,h*.46,0,w*.48,h*.46,Math.max(w,h)*.78);bg.addColorStop(0,'#101925');bg.addColorStop(.62,'#070d15');bg.addColorStop(1,'#020509');g.fillStyle=bg;g.fillRect(0,0,w,h);drawDecorativeField(g,scene,w,h,{opacity:.78});const cx=w*.5,cy=h*.51,maxOrbit=Math.max(58,Math.min(w,h)*.39);g.strokeStyle='rgba(158,177,193,.20)';g.lineWidth=1;for(const body of scene.bodies||[]){const p=bodyPoint(body,cx,cy,maxOrbit);g.beginPath();g.ellipse(cx,cy,p.radius,p.radius*.55,0,0,Math.PI*2);g.stroke()}const spread=Math.min(24,Math.min(w,h)*.035);for(let i=0;i<(scene.stars||[]).length;i++){const off=i===0?0:((i%2?1:-1)*Math.ceil(i/2)*spread);drawStar(g,scene.stars[i],cx+off,cy+(i===0?0:spread*.18),i===0?1:.72)}for(const body of scene.bodies||[]){const p=bodyPoint(body,cx,cy,maxOrbit),selected=body.orbitSlot===state.selectedOrbitSlot;g.fillStyle=body.descriptor.palette.marker;g.beginPath();g.arc(p.x,p.y,selected?6.4:4.2,0,Math.PI*2);g.fill();if(selected){g.strokeStyle='rgba(218,232,242,.88)';g.lineWidth=1.5;g.beginPath();g.arc(p.x,p.y,12.5,0,Math.PI*2);g.stroke()}}if(transitionProgress!==null){const selected=(scene.bodies||[]).find(b=>b.orbitSlot===state.selectedOrbitSlot);if(selected){const p=bodyPoint(selected,cx,cy,maxOrbit),t=clamp(transitionProgress,0,1);g.strokeStyle='rgba(220,233,242,.86)';g.beginPath();g.arc(p.x+(w/2-p.x)*t,p.y+(h/2-p.y)*t,6+12*t,0,Math.PI*2);g.stroke()}}g.restore()}
function projectedPlanetRadiusPx(h){const P=root.__OFU_PLANET_PREVIEW__,snap=P?.snapshot?.(),distance=Number(snap?.camera?.distanceM),radius=Number(P?.radius);if(!(distance>radius&&radius>0))return null;return Math.max(3,Math.tan(Math.asin(clamp(radius/distance,0,.999999)))/Math.tan(55*Math.PI/360)*h/2)}
function drawPlanetAnnotations(g,scene,w,h,stage,alpha=1){const body=(scene?.bodies||[]).find(b=>b.orbitSlot===state.selectedOrbitSlot),rp=projectedPlanetRadiusPx(h);if(!body||!(rp>2))return;const p=body.descriptor.palette,cx=w/2,cy=h/2;g.save();g.globalAlpha=.34*alpha;g.strokeStyle=rgba(p.accent,.72);g.lineWidth=Math.max(1,Math.min(2,rp*.006));g.beginPath();g.arc(cx,cy,Math.max(1,rp+2),0,Math.PI*2);g.stroke();g.globalAlpha=.7*alpha;g.fillStyle='rgba(210,222,231,.72)';g.font='600 10px ui-monospace,SFMono-Regular,Menlo,monospace';g.textAlign='right';g.fillText(String(body.descriptor.presentationFamily||body.descriptor.bulkPriorClass)+' · PRESENTATION',w-14,20);g.restore()}
function drawPlanetStage(g,scene,w,h,stage,alpha=1){drawDecorativeField(g,scene,w,h,{excludeCenter:stage==='orbit'?.52:stage==='approach'?.88:1.05,opacity:.6*alpha});drawPlanetAnnotations(g,scene,w,h,stage,alpha)}
function transitionState(now){const tr=state.transition;if(!tr)return null;const t=clamp((now-tr.startedAt)/Math.max(1,tr.durationMs),0,1);if(t>=1){state.transition=null;return null}return{...tr,t}}
function render(now=performance.now()){if(!resize())return;const g=state.ctx,w=state.viewport.cssWidth,h=state.viewport.cssHeight;clear();const tr=transitionState(now);if(tr?.from==='system'&&tr.to!=='system'){drawPlanetStage(g,state.scene,w,h,tr.to,tr.t);drawSystem(g,state.scene,w,h,1-tr.t,tr.t)}else if(tr?.to==='system'&&tr.from!=='system'){drawPlanetStage(g,state.scene,w,h,tr.from,1-tr.t);drawSystem(g,state.scene,w,h,tr.t,null)}else if(state.stage==='system')drawSystem(g,state.scene,w,h);else drawPlanetStage(g,state.scene,w,h,state.stage);state.frames++;state.lastRenderAt=now;if(state.transition)root.requestAnimationFrame(render)}
function setSystemScene(scene){if(!scene||scene.authority!==AUTHORITY)throw new Error('presentation-only system scene required');state.scene=scene;render();return snapshot()}
function setSelectedBody(selector){let slot;if(typeof selector==='number')slot=selector;else if(typeof selector==='bigint')slot=Number(selector);else if(selector&&selector.orbitSlot!==undefined)slot=Number(selector.orbitSlot);else if(selector?.canonicalId&&state.scene)slot=state.scene.bodies.find(b=>b.canonicalId===selector.canonicalId)?.orbitSlot;if(!Number.isInteger(slot)||slot<0)throw new Error('visual selected body requires valid orbit slot or canonical id');state.selectedOrbitSlot=slot;render();return snapshot()}
function setStage(stage,{durationMs=700}={}){stage=String(stage||'').toLowerCase();if(!STAGES.includes(stage))throw new Error('unknown visual universe stage');if(stage===state.stage)return snapshot();const from=state.stage;state.stage=stage;const duration=state.reducedMotion?0:clamp(Number(durationMs)||0,0,1200);state.transition=duration>0?Object.freeze({from,to:stage,startedAt:performance.now(),durationMs:duration}):null;render();return snapshot()}
function systemSceneFromCanonical(ctx,key,opts){return U.systemSceneDescriptor(ctx,freezeKey(key),opts)}
function syncFromProduct(){const nav=O.v08ExploreNavigation?.state,P=root.__OFU_PLANET_PREVIEW__;if(!nav||!P?.ctx||!P?.chosen?.key)return false;const key=freezeKey(P.chosen.key),ks=keyString(key),slot=Number(nav.targets?.[nav.selectedIndex]?.key?.orbitSlot??P.chosen.key.orbitSlot??0);if(ks!==state.lastSystemKey){state.lastSystemKey=ks;setSystemScene(U.systemSceneDescriptor(P.ctx,key,{selectedOrbitSlot:slot}))}if(slot!==state.lastSelectedSlot){state.lastSelectedSlot=slot;setSelectedBody(slot)}const surface=P.surfaceProvider?.snapshot?.(),stage=surface?.mode==='LOCAL'?String(surface.currentBand||'LOCAL_SURFACE').toLowerCase():(STAGES.includes(nav.stage)?nav.stage:(P.regime==='SYSTEM_VIEW'?'system':P.regime==='PLANET_ORBIT'?'orbit':P.regime==='PLANET_APPROACH'?'approach':'close'));if(stage!==state.lastProductStage){state.lastProductStage=stage;setStage(stage)}return true}
function snapshot(){return Object.freeze({version:VERSION,authority:AUTHORITY,stage:state.stage,selectedOrbitSlot:state.selectedOrbitSlot,sceneStatus:state.scene?.status||null,canonicalSystemId:state.scene?.canonicalSystemId||null,canonicalBodyCount:state.scene?.bodies?.length||0,canonicalStarCount:state.scene?.stars?.length||0,decorativeStarCount:state.scene?.decorativeStarField?.points?.length||0,decorativeSelectable:false,primaryPlanetOwner:state.primaryPlanetOwner,overlayRepaintsPrimaryPlanet:false,physicalOrbitScaleClaim:false,physicalOrbitalPhaseClaim:false,transition:state.transition?Object.freeze({...state.transition}):null,viewport:state.viewport,frames:state.frames,lastRenderAt:state.lastRenderAt,reducedMotion:state.reducedMotion})}
function init(){ensureCanvas();state.reducedMotion=!!root.matchMedia?.('(prefers-reduced-motion: reduce)').matches;root.matchMedia?.('(prefers-reduced-motion: reduce)').addEventListener?.('change',e=>{state.reducedMotion=e.matches;if(e.matches)state.transition=null;render()});if(typeof ResizeObserver==='function'&&state.canvas?.parentElement){const ro=new ResizeObserver(()=>render());ro.observe(state.canvas.parentElement);state.resizeObserver=ro}else root.addEventListener?.('resize',()=>render());syncFromProduct();state.syncTimer=root.setInterval(syncFromProduct,160)}
O.v09VisualUniverse=Object.freeze({VERSION,AUTHORITY,STAGES,state,setSystemScene,setSelectedBody,setStage,systemSceneFromCanonical,presentationDescriptorForBody:U.presentationDescriptorForBody,presentationDescriptorForStar:U.presentationDescriptorForStar,snapshot,syncFromProduct});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})(typeof globalThis!=='undefined'?globalThis:this);
