import { Color3 } from '@babylonjs/core/Maths/math.color.js';
import { Mesh } from '@babylonjs/core/Meshes/mesh.js';
import { VertexData } from '@babylonjs/core/Meshes/mesh.vertexData.js';
import { TransformNode } from '@babylonjs/core/Meshes/transformNode.js';
import { StandardMaterial } from '@babylonjs/core/Materials/standardMaterial.js';
import { Scene } from '@babylonjs/core/scene.js';
import { createSurfaceTerrainSampler } from './terrain-field.js';
import { createHumanPlaceGrammar } from './human-place-grammar.js';
import { deriveProductDepthProfile } from './product-depth-profile.js';

const clamp01=value=>Math.max(0,Math.min(1,Number(value)||0));
const mix=(a,b,t)=>Number(a)+(Number(b)-Number(a))*Number(t);
const scaleDistance=(a,b)=>Math.max(Math.abs(a.x-b.x),Math.abs(a.y-b.y),Math.abs(a.z-b.z));

function buildFarField(scene,world,terrainRoot){
  const root=new TransformNode('r6-w0-human-far-field-root',scene);root.parent=terrainRoot;const material=new StandardMaterial('r6-w0-human-far-field-material',scene);material.useVertexColors=true;material.specularColor=new Color3(.025,.035,.04);material.emissiveColor=new Color3(.035,.045,.04);material.backFaceCulling=false;
  const heightAt=createSurfaceTerrainSampler({surfaceTarget:world.terrainTarget,radiusM:world.terrainTarget.radiusM,seed:world.terrainTarget.seed,profile:world.terrainTarget.profile}),regime=world.generative?.presentation?.regime||{},place=createHumanPlaceGrammar({seed:world.generative?.seeds?.local||world.bodyId,regime,terrainSampler:heightAt,cellSizeM:32}),depthProfile=deriveProductDepthProfile({world,place}),palette=world.generative?.presentation?.terrainPalette||{},low=palette.low||[.25,.32,.28],mid=palette.mid||[.38,.48,.3],high=palette.high||[.68,.61,.52],rings=[180,420,860,1600],segments=64,positions=[],normals=[],colors=[],indices=[],fogColor=depthProfile.visual.fogColor;
  for(let ring=0;ring<rings.length;ring++){
    const radius=rings[ring],fade=1-ring/(rings.length-1),tone=ring<2?mid:ring===2?low:high,depthT=ring/(rings.length-1),fogBlend=clamp01(depthT*(depthProfile.visual.hazeOpacity*.48+depthProfile.visual.aerialStrength*.22)),contrast=1+(depthProfile.visual.farFieldContrast-1)*(.42+.58*depthT);
    for(let index=0;index<segments;index++){
      const angle=index/segments*Math.PI*2,east=Math.cos(angle)*radius,north=Math.sin(angle)*radius,height=heightAt(east,north),reliefCue=clamp01(.5+height/Math.max(120,world.terrainTarget.profile?.macroAmplitudeM||800)),mixed=tone.map((value,channel)=>mix(value,fogColor[channel],fogBlend)),neutral=(mixed[0]+mixed[1]+mixed[2])/3,rgb=mixed.map(value=>clamp01(neutral+(value-neutral)*contrast)),alpha=clamp01((.14+.68*fade)*(1-fogBlend*.34));positions.push(east,height-.08,north);normals.push(0,1,0);colors.push(rgb[0]*(.78+.22*reliefCue),rgb[1]*(.78+.22*reliefCue),rgb[2]*(.78+.22*reliefCue),alpha)
    }
  }
  const at=(ring,index)=>ring*segments+(index+segments)%segments;for(let ring=0;ring<rings.length-1;ring++)for(let index=0;index<segments;index++){const next=(index+1)%segments,a=at(ring,index),b=at(ring,next),c=at(ring+1,index),d=at(ring+1,next);indices.push(a,c,b,b,c,d)}
  const mesh=new Mesh('r6-w0-human-far-field',scene),data=new VertexData();data.positions=positions;data.normals=normals;data.colors=colors;data.indices=indices;data.applyToMesh(mesh,true);mesh.material=material;mesh.parent=root;mesh.isPickable=false;mesh.hasVertexAlpha=true;material.alpha=depthProfile.visual.farFieldAlpha;mesh.metadata={authority:'PRESENTATION_ONLY',source:'SAME_BODY_FIXED_TERRAIN_FIELD',scientificLandformClaim:false,bodyId:world.bodyId,surfaceId:world.surfaceId,outerRadiusM:rings.at(-1),vertexCount:positions.length/3,bounded:true,productDepthContract:depthProfile.contract};return{root,mesh,material,place,heightAt,depthProfile};
}

export function createHumanRendererConvergence({base,getWorld}={}){
  if(!base?.scene||typeof getWorld!=='function')throw new TypeError('HUMAN convergence requires the existing renderer scene and world');
  const scene=base.scene,fogBaseline=Object.freeze({mode:scene.fogMode,start:scene.fogStart,end:scene.fogEnd,density:scene.fogDensity,color:scene.fogColor?.clone?.()||new Color3(0,0,0)}),materialBaselines=new Map();let farField=null,surfaceKey='',disposed=false,stableScaleCorrections=0,lastKey='',presentationActive=false,fogApplications=0,fogRestorations=0,lastAppliedFog=null;
  const rememberMaterial=material=>{if(!material||materialBaselines.has(material))return;materialBaselines.set(material,Object.freeze({alpha:material.alpha,specularPower:material.specularPower,specularColor:material.specularColor?.clone?.()||Color3.Black(),emissiveColor:material.emissiveColor?.clone?.()||Color3.Black()}))};
  const restorePresentation=({clear=false}={})=>{let changed=false;if(presentationActive||scene.fogMode!==fogBaseline.mode){scene.fogMode=fogBaseline.mode;scene.fogStart=fogBaseline.start;scene.fogEnd=fogBaseline.end;scene.fogDensity=fogBaseline.density;if(scene.fogColor?.copyFrom)scene.fogColor.copyFrom(fogBaseline.color);else scene.fogColor=fogBaseline.color.clone();changed=true;fogRestorations++}for(const [material,baseline] of materialBaselines){try{material.alpha=baseline.alpha;material.specularPower=baseline.specularPower;material.specularColor.copyFrom(baseline.specularColor);material.emissiveColor.copyFrom(baseline.emissiveColor);changed=true}catch{/* Base rebind/dispose may already have retired this presentation material. */}}if(clear)materialBaselines.clear();presentationActive=false;lastAppliedFog=null;return changed};
  const disposeFarField=()=>{if(!farField)return;try{farField.root.dispose(false,true)}catch{}try{farField.material.dispose(true,true)}catch{}farField=null};
  const applyProductDepth=snapshot=>{
    if(!farField)return false;const profile=farField.depthProfile,visual=profile.visual,metresPerRenderUnit=Math.max(.000001,Math.abs(Number(snapshot?.camera?.metresPerRenderUnit)||1)),density=visual.fogDensityPerM*metresPerRenderUnit,color=visual.fogColor;
    scene.fogMode=Scene.FOGMODE_EXP2;scene.fogDensity=density;scene.fogColor.set(color[0],color[1],color[2]);farField.material.alpha=visual.farFieldAlpha;
    const rockMaterial=scene.getMaterialByName('rock-field-material');if(rockMaterial){rememberMaterial(rockMaterial);const specular=visual.localSpecular,patina=visual.localPatina,thermal=visual.thermalStress,e=.018+patina*.045+thermal*.022;rockMaterial.alpha=clamp01(.7+visual.horizonClarity*.12-visual.hazeOpacity*.06);rockMaterial.specularPower=18+specular*72;rockMaterial.specularColor.set(.025+specular*.16,.03+specular*.18,.035+specular*.2);rockMaterial.emissiveColor.set(color[0]*e,color[1]*e,color[2]*e)}
    presentationActive=true;fogApplications++;lastAppliedFog=Object.freeze({mode:'EXP2',density:Number(density.toFixed(8)),densityPerM:visual.fogDensityPerM,metresPerRenderUnit:Number(metresPerRenderUnit.toFixed(8)),color:Object.freeze([...color])});return true
  };
  const update=snapshot=>{
    if(disposed)return Object.freeze({changed:false});const world=getWorld(),humanWeight=Number(snapshot.representationHandoff?.weights?.HUMAN)||0,localWeight=Number(snapshot.representationHandoff?.weights?.LOCAL_SURFACE)||0,active=humanWeight>.001,key=`${world.bodyId}::${world.surfaceId}::${world.terrainTarget?.seed||''}`,terrainRoot=scene.getTransformNodeByName('terrain-patches-root');let changed=false;
    if(active&&terrainRoot&&key!==surfaceKey){restorePresentation({clear:true});disposeFarField();farField=buildFarField(scene,world,terrainRoot);surfaceKey=key;changed=true}
    if(farField){farField.root.setEnabled(active);farField.material.alpha=active?farField.depthProfile.visual.farFieldAlpha:0}
    if(active&&farField){changed=applyProductDepth(snapshot)||changed;for(const mesh of scene.meshes.filter(item=>item.name.startsWith('selectable-local-object-')&&item.metadata?.baseScale)){const encoded=Number(mesh.metadata.baseScale)||1,physical=encoded/(mesh.metadata.sample?1.35:1),desired=physical*(mesh.metadata.sample?1.12:1);if(Math.abs(mesh.scaling.x-desired)>.0001||Math.abs(mesh.scaling.y-desired)>.0001||Math.abs(mesh.scaling.z-desired)>.0001){mesh.scaling.setAll(desired);stableScaleCorrections++;changed=true}}const localRoot=scene.getTransformNodeByName('local-detail-root');if(localRoot)localRoot.setEnabled(true)}else if(presentationActive)changed=restorePresentation()||changed;
    const profile=farField?.depthProfile||null,visualKey=[key,active,humanWeight.toFixed(4),localWeight.toFixed(4),profile?.causalFingerprint||'none',profile?.visual?.fogDensityPerM||0,stableScaleCorrections].join('|');changed=changed||visualKey!==lastKey;lastKey=visualKey;return Object.freeze({changed,active,stableScaleCorrections,productDepth:profile,farField:farField?Object.freeze({outerRadiusM:farField.mesh.metadata.outerRadiusM,vertexCount:farField.mesh.metadata.vertexCount,bounded:true,horizonReliefM:farField.place.horizon.reliefM,aerialPerspective:farField.place.aerialPerspective,authority:'PRESENTATION_ONLY'}):null})
  };
  const cancel=()=>{const restored=restorePresentation({clear:true});disposeFarField();surfaceKey='';lastKey='';return restored||true};
  const snapshot=()=>Object.freeze({contract:'ofu-r6-w1-product-depth-human-renderer-1',active:!!farField?.root.isEnabled(),stableWorldScale:true,stableScaleCorrections,farField:farField?Object.freeze({bodyId:farField.mesh.metadata.bodyId,surfaceId:farField.mesh.metadata.surfaceId,outerRadiusM:farField.mesh.metadata.outerRadiusM,vertexCount:farField.mesh.metadata.vertexCount,bounded:true,authority:'PRESENTATION_ONLY'}):null,productDepth:farField?.depthProfile||null,appliedFog:lastAppliedFog,fogApplications,fogRestorations,selectedSampleScalePolicy:'BASE_PHYSICAL_SCALE_PLUS_1.12_SELECTION_CUE',depthBandPolicy:'DETAIL_HAZE_PRIORITY_NOT_WORLD_SCALE'});
  const dispose=()=>{if(disposed)return false;restorePresentation({clear:true});disposed=true;disposeFarField();return true};return Object.freeze({update,cancel,snapshot,dispose});
}
