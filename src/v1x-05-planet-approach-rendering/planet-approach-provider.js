(function(root){
'use strict';
const O=root.OFU=root.OFU||{};
const CONTRACT='ofu-v1x05-planet-approach-provider-1';
const VERSION='1.0.0';
const AUTHORITY='PRESENTATION_ONLY';
const PHYSICAL_CONTRACT='ofu-p5-planet-physical-v1';
const ENVIRONMENT_CONTRACT='ofu-p5-p6-environment-v2';
const MAX_TRACE_SAMPLES=1025;
const SOURCE_CLASSES=Object.freeze(new Set(['CANONICAL_PROVEN','MODEL_DERIVED_SIMULATION']));
const EPS=1e-12;
function fail(message){throw new Error('V1X-05: '+message);}
function freeze(value){if(!value||typeof value!=='object'||Object.isFrozen(value))return value;if(ArrayBuffer.isView(value))return value;for(const key of Object.keys(value))freeze(value[key]);return Object.freeze(value);}
function cloneOpaque(value){if(value==null||typeof value!=='object')return value;if(Array.isArray(value))return value.map(cloneOpaque);if(ArrayBuffer.isView(value))return Array.from(value);const out={};for(const key of Object.keys(value))out[key]=cloneOpaque(value[key]);return out;}
function finite(value,name){const n=Number(value);if(!Number.isFinite(n))fail(name+' must be finite');return n;}
function positive(value,name){const n=finite(value,name);if(!(n>0))fail(name+' must be positive');return n;}
function boundedInteger(value,min,max,name){const n=Number(value);if(!Number.isInteger(n)||n<min||n>max)fail(name+' out of range');return n;}
function vector3(value,name){if(!Array.isArray(value)||value.length!==3)fail(name+' must be a vec3');const v=value.map((x,i)=>finite(x,name+'['+i+']'));return v;}
function add(a,b){return [a[0]+b[0],a[1]+b[1],a[2]+b[2]];}
function sub(a,b){return [a[0]-b[0],a[1]-b[1],a[2]-b[2]];}
function mul(a,s){return [a[0]*s,a[1]*s,a[2]*s];}
function dot(a,b){return a[0]*b[0]+a[1]*b[1]+a[2]*b[2];}
function cross(a,b){return [a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]];}
function norm(a){return Math.hypot(a[0],a[1],a[2]);}
function unit(a,name='vector'){const n=norm(a);if(!(n>EPS))fail(name+' has no direction');return mul(a,1/n);}
function clamp(v,lo,hi){return Math.max(lo,Math.min(hi,v));}
function lerp(a,b,t){return a+(b-a)*t;}
function smoothstep(t){const x=clamp(t,0,1);return x*x*(3-2*x);}
function chooseOrthogonal(v){const av=v.map(Math.abs);const basis=av[0]<=av[1]&&av[0]<=av[2]?[1,0,0]:av[1]<=av[2]?[0,1,0]:[0,0,1];return unit(cross(v,basis),'antipodal tangent');}
function slerpUnit(a,b,t){const d=clamp(dot(a,b),-1,1);if(d>0.999999){return unit(add(mul(a,1-t),mul(b,t)),'slerp');}if(d<-0.999999){const tangent=chooseOrthogonal(a),angle=Math.PI*t;return unit(add(mul(a,Math.cos(angle)),mul(tangent,Math.sin(angle))),'antipodal slerp');}const angle=Math.acos(d),sin=Math.sin(angle);return unit(add(mul(a,Math.sin((1-t)*angle)/sin),mul(b,Math.sin(t*angle)/sin)),'slerp');}
function numberFromExact(value,name){if(typeof value==='bigint'){const limit=BigInt(Number.MAX_SAFE_INTEGER);if(value>limit||value<-limit)fail(name+' exceeds exact Number range');return Number(value);}return finite(value,name);}
function physicalFacts(payload){if(!payload||payload.contractId!==PHYSICAL_CONTRACT||payload.status!=='SUPPORTED'||!payload.physical)fail('supported '+PHYSICAL_CONTRACT+' payload required');const radiusM=positive(numberFromExact(payload.physical.meanRadiusM,'meanRadiusM'),'meanRadiusM');const gravityMicro=positive(numberFromExact(payload.physical.surfaceGravityMicroMs2,'surfaceGravityMicroMs2'),'surfaceGravityMicroMs2');return freeze({radiusM,surfaceGravityMs2:gravityMicro/1e6,sourceContract:PHYSICAL_CONTRACT,sourceAuthorityClass:'CANONICAL_PROVEN',presentationAuthorityClass:AUTHORITY});}
function modelCue(kind,cue){if(!cue)return freeze({kind,enabled:false,reason:'NO_SUPPORTED_SOURCE',sourceAuthorityClass:null,presentationAuthorityClass:AUTHORITY,scientificEvidence:false});if(!SOURCE_CLASSES.has(cue.authorityClass)||cue.supported!==true||typeof cue.sourceId!=='string'||cue.sourceId.length===0)return freeze({kind,enabled:false,reason:'SOURCE_NOT_AUTHORIZED',sourceAuthorityClass:cue.authorityClass||null,presentationAuthorityClass:AUTHORITY,scientificEvidence:false});return freeze({kind,enabled:true,reason:'SUPPORTED_SOURCE',sourceAuthorityClass:cue.authorityClass,sourceId:cue.sourceId,claim:typeof cue.claim==='string'?cue.claim:null,presentationAuthorityClass:AUTHORITY,scientificEvidence:false});}
function cuePolicy(environment=null,modelCues={}){let atmosphere=freeze({kind:'atmosphere',enabled:false,reason:'UNKNOWN_OR_UNSUPPORTED',sourceAuthorityClass:null,presentationAuthorityClass:AUTHORITY,scientificEvidence:false});if(environment&&environment.contractId===ENVIRONMENT_CONTRACT&&environment.atmosphere&&environment.atmosphere.epistemicStatus==='KNOWN'){const retained=numberFromExact(environment.atmosphere.atmosphericRetainedMassTg??0,'atmosphericRetainedMassTg');atmosphere=freeze({kind:'atmosphere',enabled:retained>0,reason:retained>0?'CANONICAL_KNOWN_RETAINED_MASS':'CANONICAL_KNOWN_ZERO_RETAINED_MASS',sourceAuthorityClass:'CANONICAL_PROVEN',sourceId:environment.atmosphere.provenance||ENVIRONMENT_CONTRACT,presentationAuthorityClass:AUTHORITY,scientificEvidence:false});}else if(modelCues.atmosphere){atmosphere=modelCue('atmosphere',modelCues.atmosphere);}return freeze({atmosphere,water:modelCue('water',modelCues.water),ice:modelCue('ice',modelCues.ice),material:modelCue('material',modelCues.material),policy:'VISIBLE_ONLY_WHEN_SOURCE_AUTHORIZED',authorityClass:AUTHORITY});}
function normalizedLongitude(deg){let x=finite(deg,'longitudeDeg');x=((x+180)%360+360)%360-180;return x===-180?180:x;}
function surfaceTarget({planetId,latitudeDeg,longitudeDeg,radiusM,surfaceTargetReferenceFrameId=null}){if(typeof planetId!=='string'||planetId.length===0||planetId.includes('/nav-anchor/'))fail('stable planetId string required');const lat=finite(latitudeDeg,'latitudeDeg');if(lat<-90||lat>90)fail('latitudeDeg out of range');const lon=normalizedLongitude(longitudeDeg),phi=lat*Math.PI/180,lambda=lon*Math.PI/180;const direction=unit([Math.cos(phi)*Math.cos(lambda),Math.sin(phi),Math.cos(phi)*Math.sin(lambda)],'surface direction');const q=direction.map(v=>Math.round(v*1e9));return freeze({planetId,latitudeDeg:lat,longitudeDeg:lon,directionPlanetLocal:direction,pointOnMeanRadiusM:mul(direction,positive(radiusM,'radiusM')),anchorToken:planetId+'/nav-anchor/'+q.join(','),referenceFrameId:surfaceTargetReferenceFrameId,projection:'GEOCENTRIC_PRESENTATION_MEAN_RADIUS',authorityClass:AUTHORITY,canonicalGeodesyClaim:false,elevationClaim:false});}
function splitScalar(value){const n=finite(value,'split value'),hi=Math.fround(n),lo=n-hi;return freeze({hi,lo});}
function splitVec3(value){const v=vector3(value,'split vector');return freeze({hi:v.map(x=>Math.fround(x)),lo:v.map(x=>x-Math.fround(x))});}
function reconstructSplit(pair){return pair.hi.map((v,i)=>v+pair.lo[i]);}
function stageFor(radiusRatio,last){if(last)return 'SURFACE_TARGET';if(radiusRatio>1000)return 'SYSTEM_HANDOFF';if(radiusRatio>50)return 'FAR_APPROACH';if(radiusRatio>8)return 'ORBIT_CONTEXT';if(radiusRatio>1.5)return 'APPROACH';return 'DESCENT';}
function buildApproachTrace(input={}){const selected=input.selectedCanonicalTarget;if(typeof selected!=='string'||selected.length===0)fail('selectedCanonicalTarget required');const facts=physicalFacts(input.physicalPlanet);const handoff=input.frameHandoff;if(!handoff||typeof handoff!=='object')fail('frameHandoff required');if(handoff.planetId!==selected)fail('frameHandoff planet identity mismatch');if(typeof handoff.systemReferenceFrameId!=='string'||!handoff.systemReferenceFrameId)fail('systemReferenceFrameId required');if(typeof handoff.planetReferenceFrameId!=='string'||!handof.planetReferenceFrameId)fail('planetReferenceFrameId required');const start=vector3(handoff.cameraPlanetLocalM,'cameraPlanetLocalM'),startRadius=norm(start);if(!(startRadius>facts.radiusM))fail('camera must begin above mean radius');const sampleCount=boundedInteger(input.samples??257,2,MAX_TRACE_SAMPLES,'samples');const handoffAltitudeM=input.surfaceHandoffAltitudeM==null?clamp(facts.radiusM
*0.002,500,100000):positive(input.surfaceHandoffAltitudeM,'surfaceHandoffAltitudeM');const finalRadius=facts.radiusM+handoffAltitudeM;if(!(startRadius>finalRadius))fail('camer starts inside requested surface handoff radius');const target=surfaceTarget({planetId:selected,latitudeDeg:input.surfaceTarget?.latitudeDeg??0,longitudeDeg:input.surfaceTarget?.longitudeDeg??0,radiusM:facts.radiusM,surfaceTargetReferenceFrameId:input.surfaceTarget?.surfaceTargetReferenceFrameId??null});const cues=cuePolicy(input.environment||null,input.modelCues||{}),startDirection=unit(start,'cameraPlanetLocalM'),targetDirection=target.directionPlanetLocal,frames=[];let maxStepM=0,maxSplitReconstructionErrorM=0,previous=null,previousRadius=Infinity;for(let i=0;i<sampleCount;i++){const t=i/(sampleCount-1),s=smoothstep(t),logRadius=lerp(Math.log(startRadius),Math.log(finalRadius),s),radius=Math.exp(logRadius),direction=slerpUnit(startDirection,targetDirection,s);let position=mul(direction,radius);if(i===0)position=start.slice();if(i===sampleCount-1)position=mul(targetDirection,finalRadius);const split=splitVec3(position),reconstructed=reconstructSplit(split),splitError=norm(sub(position,reconstructed));maxSplitReconstructionErrorM=Math.max(maxSplitReconstructionErrorM,splitError);if(previous){maxStepM=Math.max(maxStepM,norm(sub(position,previous)));if(radius>previousRadius+Math.max(1K6,000001,previousRadius*1e-12))fail('approach radius is not monotonic');}const ratio=radius/facts.radiusM;frames.push(freeze({index:i,t,stage:stageFor(ratio,i===sampleCount-1),selectedCanonicalTarget:selected,referenceFrameId:handoff.planetReferenceFrameId,positionPlanetLocalM:position,positionHighLowM:split,lookAtPlanetLocalM:[0,0,0],altitudeAboveMeanRadiusM:Math.max(0,radius-facts.radiusM),authorityClass:AUTHORITY}));previous=position;previousRadius=radius;}
const first=frames[0],last=frames[frames.length-1];return freeze({contract:CONTRACT,version:VERSION,authorityClass:AUTHORITY,selectedCanonicalTarget:selected,physicalFacts:facts,cues,surfaceTarget:target,systemHandoff:freeze({selectedCanonicalTarget:selected,referenceFrameId:handoff.systemReferenceFrameId,planetReferenceFrameId:handoff.planetReferenceFrameId,transformPreappliedByUpstream:true,systemContext:cloneOpaque(handoff.systemContext??null)}),frames,continuity:freeze({model:'SMOOTHSTEP_LOG_RADIUS_GREAT_CIRCLE',sampleCount,maxStepM,maxSplitReconstructionErrorM,monotonicRadialDescent:true,teleportOperationCount:0,startPositionPlanetLocalM:first.positionPlanetLocalM,endPositionPlanetLocalM:last.positionPlanetLocalM}),referenceFrameWitness:freeze([{stage:'SYSTEM_HANDOFF',selectedCanonicalTarget:selected,referenceFrameId:handoff.systemReferenceFrameId},{stage:'ORBIT_APPROACH',selectedCanonicalTarget:selected,referenceFrameId:handoff.planetReferenceFrameId},{stage:'SURFACE_TARGET',selectedCanonicalTarget:selected,referenceFrameId:target.referenceFrameId||handoff.planetReferenceFrameId,anchorToken:target.anchorToken}]),resourcePolicy:freeze({maxTraceSamples:MAX_TRACE_SAMPLES,absoluteSystemCoordinatesConsumed:false,planetLocalHighLowEncoding:true}),integration:freeze({cameraStateMutation:false,selectionMutation:false,canonicalPromotion:false,upstreamFrameTransformRequired:true})});}
function reverseExit(packet){if(!packet||packet.contract!==CONTRACT||!Array.isArray(packet.frames)||packet.frames.length<2)fail('approach packet required');const selected=packet.selectedCanonicalTarget,frames=packet.frames.slice().reverse().map((frame,index)=>freeze({...frame,index,t:index/(packet.frames.length-1),stage:index===packet.frames.length-1?'SYSTEM_RETURN_HANDOFF':'REVERSE_EXIT'}));return freeze({contract:CONTRACT,version:VERSION,authorityClass:AUTHORITY,direction:'EXIT_TO_SYSTEM',selectedCanonicalTarget:selected,frames,exitHandoff:packet.systemHandoff,selectionPreserved:frames.every(frame=>frame.selectedCanonicalTarget===selected),canonicalPromotion:false});}
const GLOBE_VERTEX_SHADER`#version 300 es\nprecision highp float;\nlayout(location=0) in vec3 aPosition;\nlayout(location=1) in vec3 aNormal;\nuniform mat4 uMvp;\nout vec3 vNormal;\nvoid main(){vNormal=normalize(aNormal);gl_Position=uMvp*vec4(aPosition,1.0);}`;
const GLOBE_FRAGMENT_SHADER`#version 300 es\nprecision highp float;\nin vec3 vNormal;\nuniform vec3 uLightDirection;\nuniform float uAtmosphereCue;\nuniform float uWaterCue;\nuniform float uIceCue;\nuniform float uMaterialCue;\nout vec4 outColor;\nvoid main(){float lit=0.20+0.80*max(dot(normalize(vNormal),normalize(uLightDirection)),0.0);vec3 neutral=vec3(0.42,0.43,0.45);vec3 supported=neutral;\nsupported=mix(supported,vec3(0.20,0.38,0.56),clamp(uWaterCue,0.0,1.0)*0.28);\nsupported=mix(supported,vec3(0.78,0.84,0.89),clamp(uIceCue,0.0,1.0)*0.25);\nsupported=mix(supported,vec3(0.45,0.36,0.29),clamp(uMaterialCue,0.0,1.0)*0.18);\nfloat rim=pow(1.0-max(dot(normalize(vNormal),vec3(0.0,0.0,1.0)),0.0),3.0)*clamp(uAtmosphereCue,0.0,1.0);outColor=vec4(supported*lit+vec3(0.18,0.34,0.58)*rim,1.0);}`;
function shaderCueUniforms(cues){return freeze({uAtmosphereCue:cues.atmosphere.enabled?1:0,uWaterCue:cues.water.enabled?1:0,uIceCue:cues.ice.enabled?1:0,uMaterialCue:cues.material.enabled?1:0,authorityClass:AUTHORITY,scientificEvidence:false});}
const provider=freeze({id:'v1x-05-planet-approach-rendering.planet-approach',contract:CONTRACT,version:VERSION,authorityClass:AUTHORITY,canonicalPromotion:false,operations:freeze(['PROJECT']),project:buildApproachTrace,reverseExit,physicalFacts,cuePolicy,surfaceTarget,splitVec3,shaderCueUniforms,shaders:freeze({vertex:GLOBE_VERTEX_SHADER,fragment:GLOBE_FRAGMENT_SHADER})});
O.v1x05PlanetApproach=provider;
})(globalThis);
