(function(root){
'use strict';const O=root.OFU=root.OFU||{},VERSION='ofu-v1-world-webgl2-1',AUTHORITY='PRESENTATION_ONLY';
function compile(gl,type,source){const s=gl.createShader(type);gl.shaderSource(s,source);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS)){const log=gl.getShaderInfoLog(s)||'shader compile failed';gl.deleteShader(s);throw new Error(log)}return s}
function link(gl,vs,fs){const p=gl.createProgram();let a=null,b=null;try{a=compile(gl,gl.VERTEX_SHADER,vs);b=compile(gl,gl.FRAGMENT_SHADER,fs);gl.attachShader(p,a);gl.attachShader(p,b);gl.linkProgram(p);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(p)||'program link failed');return p;}catch(error){gl.deleteProgram(p);throw error;}finally{if(a)gl.deleteShader(a);if(b)gl.deleteShader(b);}}
function create(canvas,{maxDpr=2,mobile=false}={}){if(!canvas?.getContext)throw new TypeError('canvas required');if(!O.v1WorldShaders)throw new Error('v1 world shaders required');const budget=O.v1RenderBudget?.create({mobile,dpr:Math.min(maxDpr,root.devicePixelRatio||1)})||null;let gl=null,point=null,globe=null,buffer=null,texture=null,textureKey=null,lost=false,last=null,frame=0;const metrics={frames:0,drawCalls:0,restores:0,resizes:0};function init(){gl=canvas.getContext('webgl2',{alpha:true,antialias:true,premultipliedAlpha:false,preserveDrawingBuffer:false});if(!gl)throw new Error('WebGL2 unavailable');try{point=link(gl,O.v1WorldShaders.pointsVertex,O.v1WorldShaders.pointsFragment);globe=link(gl,O.v1WorldShaders.globeVertex,O.v1WorldShaders.globeFragment);}catch(error){if(point)gl.deleteProgram(point);if(globe)gl.deleteProgram(globe);point=globe=null;gl=null;throw error;}buffer=gl.createBuffer();texture=gl.createTexture();textureKey=null;gl.bindTexture(gl.TEXTURE_2D,texture);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,1,1,0,gl.RGBA,gl.UNSIGNED_BYTE,new Uint8Array([80,100,120,255]));gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.enable(gl.BLEND);gl.blendFunc(gl.SRC_ALPHA,gl.ONE_MINUS_SRC_ALPHA);lost=false}function resize(){const dpr=Math.min(maxDpr,Math.max(1,root.devicePixelRatio||1)),w=Math.max(1,Math.floor((canvas.clientWidth||canvas.width||1)*dpr)),h=Math.max(1,Math.floor((canvas.clientHeight||canvas.height||1)*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;metrics.resizes++}gl.viewport(0,0,w,h)}function drawPoints(scene){const rows=scene.objects.filter(o=>Number.isFinite(Number(o.visual?.x)));if(!rows.length)return;const data=[];for(const o of rows){const v=o.visual,c=v.color||[.72,.76,.84];data.push(Number(v.x),Number(v.y),Number(v.depth||.5),Number(c[0]),Number(c[1]),Number(c[2]),Number(v.opacity??1),Number(v.sizePx||v.radiusPx||10))}const a=new Float32Array(data);gl.useProgram(point);gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.bufferData(gl.ARRAY_BUFFER,a,gl.DYNAMIC_DRAW);for(const [name,size,offset] of [['a_position',3,0],['a_color',4,12],['a_size',1,28]]){const loc=gl.getAttribLocation(point,name);if(loc>=0){gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,size,gl.FLOAT,false,32,offset)}}gl.drawArrays(gl.POINTS,0,rows.length);metrics.drawCalls++;budget?.request('COSMIC','points:'+scene.scale,{bytes:a.byteLength,objects:rows.length,draws:1},2)}function drawGlobe(scene){const o=scene.objects.find(x=>x.kind==='PLANET_GLOBE');if(!o)return;const v=o.visual;gl.useProgram(globe);const set3=(n,x)=>gl.uniform3fv(gl.getUniformLocation(globe,n),new Float32Array(x)),set1=(n,x)=>gl.uniform1f(gl.getUniformLocation(globe,n),Number(x));set3('u_primary',v.primaryColor);set3('u_secondary',v.secondaryColor);set3('u_ocean',v.oceanColor);set3('u_ice',v.iceColor);set1('u_water',Number(v.waterAreaPpm||0)/1e6);set1('u_iceArea',Number(v.iceAreaPpm||0)/1e6);set1('u_atmosphere',v.atmosphereStrength||0);set1('u_relief',Number(v.reliefCuePpm||0)/1e6);set1('u_seed',v.seed||0);set1('u_aspect',canvas.width/canvas.height);set1('u_scale',scene.globeScale||.76);set1('u_yaw',scene.camera?.yaw||0);set1('u_pitch',scene.camera?.pitch||0);
 const map=scene.surfaceTexture;set1('u_hasSurfaceMap',map?1:0);gl.activeTexture(gl.TEXTURE0);gl.bindTexture(gl.TEXTURE_2D,texture);gl.uniform1i(gl.getUniformLocation(globe,'u_surfaceMap'),0);
 if(map&&textureKey!==map.key){if(map.width*map.height>16384||map.data.length!==map.width*map.height*4)throw new Error('Bounded world texture required');gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,map.width,map.height,0,gl.RGBA,gl.UNSIGNED_BYTE,map.data);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_S,gl.REPEAT);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_T,gl.CLAMP_TO_EDGE);textureKey=map.key;}
 gl.drawArrays(gl.TRIANGLES,0,6);metrics.drawCalls++;budget?.request('GLOBE','globe:'+scene.sourceId,{bytes:4096,objects:1,draws:1},5)}function render(scene){if(lost)return{status:'CONTEXT_LOST'};if(!gl)init();last=scene;resize();metrics.frames++;frame++;gl.clearColor(.008,.014,.028,1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);if(['PLANET','APPROACH'].includes(scene.scale))drawGlobe(scene);else drawPoints(scene);const error=gl.getError();if(error!==gl.NO_ERROR)throw new Error('WebGL draw error '+error);return{status:'RENDERED',frame,scale:scene.scale,authority:AUTHORITY,measurements:{...metrics},budget:budget?.snapshot()||null}}function onLost(e){e?.preventDefault?.();lost=true;budget?.clear('context-lost')}function onRestored(){metrics.restores++;init();if(last)render(last)}canvas.addEventListener?.('webglcontextlost',onLost,false);canvas.addEventListener?.('webglcontextrestored',onRestored,false);function dispose(){canvas.removeEventListener?.('webglcontextlost',onLost,false);canvas.removeEventListener?.('webglcontextrestored',onRestored,false);budget?.clear('dispose');if(gl){if(point)gl.deleteProgram(point);if(globe)gl.deleteProgram(globe);if(buffer)gl.deleteBuffer(buffer);if(texture)gl.deleteTexture(texture)}gl=null}function snapshot(){return Object.freeze({version:VERSION,authority:AUTHORITY,contextLost:lost,frame,allocatedPrograms:gl?2:0,allocatedBuffers:gl?1:0,allocatedTextures:gl?1:0,measurements:{...metrics},budget:budget?.snapshot()||null})}return Object.freeze({render,resize,dispose,snapshot})}
O.v1WorldWebGL2=Object.freeze({VERSION,AUTHORITY,create});
})(globalThis);
