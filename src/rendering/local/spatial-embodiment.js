(function(root){
'use strict';
const O=root.OFU=root.OFU||{};
const VERSION='ofu-v2x-07-local-spatial-embodiment-1';
const AUTHORITY='PRESENTATION_ONLY';
const PROFILES=Object.freeze({
  desktop:Object.freeze({maxModeledObjects:256,maxDecorativeInstances:512,maxPickTargets:256,maxOccluders:160,maxProviderCount:12,maxPlacementRadiusM:1600,gridCellM:16}),
  compact:Object.freeze({maxModeledObjects:96,maxDecorativeInstances:160,maxPickTargets:96,maxOccluders:64,maxProviderCount:8,maxPlacementRadiusM:700,gridCellM:20}),
  minimal:Object.freeze({maxModeledObjects:48,maxDecorativeInstances:64,maxPickTargets:48,maxOccluders:32,maxProviderCount:6,maxPlacementRadiusM:420,gridCellM:24})
});
function freeze(v){if(!v||typeof v!=='object'||Object.isFrozen(v))return v;for(const k of Object.keys(v))freeze(v[k]);return Object.freeze(v)}
function finite(v,label='value'){const n=Number(v);if(!Number.isFinite(n))throw new TypeError(label+' must be finite');return n}
function token(v,label='token',max=512){const s=String(v??'');if(!s||s.length>max)throw new TypeError(label+' required');return s}
function vec3(v,label='positionM'){if(!Array.isArray(v)||v.length!==3)throw new TypeError(label+' must contain three values');return Object.freeze(v.map((x,i)=>finite(x,label+'['+i+']')))}
function clamp(v,a,b){return Math.max(a,Math.min(b,v))}
function hash32(text){let h=2166136261>>>0;const s=String(text);for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return h>>>0}
function mix32(x){x^=x>>>16;x=Math.imul(x,0x7feb352d);x^=x>>>15;x=Math.imul(x,0x846ca68b);x^=x>>>16;return x>>>0}
function profile(name='desktop',override={}){const base=PROFILES[String(name).toLowerCase()]||PROFILES.desktop,out={...base};for(const key of Object.keys(base))if(override[key]!=null){const n=finite(override[key],key);if(n<=0)throw new RangeError(key+' must be positive');out[key]=Math.min(base[key],n)}return freeze(out)}
function cameraSnapshot(input){if(!input||typeof input!=='object')throw new TypeError('local camera snapshot required');const currentBand=String(input.currentBand||input.semanticScale||input.scale||'').toUpperCase();if(!['HUMAN','LOCAL_SURFACE','REGIONAL_SURFACE'].includes(currentBand))throw new Error('local embodiment requires human/local/regional surface band');const absolutePresentationPositionM=vec3(input.absolutePresentationPositionM||input.positionM||[0,0,Math.max(1,Number(input.presentationAltitudeM)||1)],'absolutePresentationPositionM');return freeze({planetId:token(input.planetId||input.selectionToken||'unknown-planet','planetId'),anchorToken:token(input.anchorToken||input.surfaceTargetToken||input.referenceFrameToken||'unbound-local-anchor','anchorToken'),currentBand,absolutePresentationPositionM,headingRad:finite(input.headingRad??0,'headingRad'),pitchRad:finite(input.pitchRad??0,'pitchRad'),sourceAuthority:String(input.authority||'UNSPECIFIED_SOURCE_AUTHORITY')})}
function distance2D(a,b){return Math.hypot(a[0]-b[0],a[1]-b[1])}
function itemAuthority(item,provider){return String(item.sourceAuthority||item.authority||provider.authority||'UNSPECIFIED_SOURCE_AUTHORITY')}
function normalizeModeled(item,provider,index,cam){const entityId=token(item.entityId||item.canonicalId||item.identity,'modeled entityId');const positionM=vec3(item.positionM||item.localPositionM,'modeled positionM');if(distance2D(positionM,cam.absolutePresentationPositionM)>provider.bounds.maxPlacementRadiusM)return null;const radiusM=clamp(finite(item.radiusM??item.scaleM??0.6,'modeled radiusM'),.05,200),heightM=clamp(finite(item.heightM??Math.max(radiusM*2,1),'modeled heightM'),.1,500);return freeze({id:entityId,pickKey:'entity:'+entityId,persistence:'MODELED_ENTITY',persistentEntity:true,providerId:provider.id,kind:token(item.kind||'MODELED_OBJECT','modeled kind',96),positionM,radiusM,heightM,geometryHint:String(item.geometryHint||'BOUNDED_PROXY'),sourceAuthority:itemAuthority(item,provider),displayAuthority:AUTHORITY,claims:freeze({persistentIdentityInvented:false,positionInvented:false,canonicalPositionClaim:item.canonicalPositionClaim===true,baseTerrainTruthOwned:false})})}
function decorativeId(provider,cam,item,index){if(item.presentationId)return token(item.presentationId,'presentationId');const seed=token(item.presentationSeed??item.seed??item.kind??'decoration','presentationSeed');return cam.anchorToken+'/presentation/'+provider.id+'/'+seed+'/'+String(index)}
function normalizeDecorative(item,provider,index,cam){const positionM=vec3(item.positionM||item.localPositionM,'decorative positionM');if(distance2D(positionM,cam.absolutePresentationPositionM)>provider.bounds.maxPlacementRadiusM)return null;const id=decorativeId(provider,cam,item,index),radiusM=clamp(finite(item.radiusM??item.scaleM??0.35,'decorative radiusM'),.03,80),heightM=clamp(finite(item.heightM??Math.max(radiusM*2,0.2),'decorative heightM'),.05,200);return freeze({id,pickKey:'presentation:'+id,persistence:'PRESENTATION_DECORATION',persistentEntity:false,providerId:provider.id,kind:token(item.kind||'DECORATIVE_FORM','decorative kind',96),positionM,radiusM,heightM,geometryHint:String(item.geometryHint||'DECORATIVE_PROXY'),sourceAuthority:itemAuthority(item,provider),displayAuthority:AUTHORITY,claims:freeze({persistentIdentityInvented:false,positionInvented:false,canonicalPositionClaim:false,baseTerrainTruthOwned:false})})}
function normalizeProvider(raw,bounds){if(!raw||typeof raw!=='object')throw new TypeError('local embodiment provider required');const id=token(raw.id,'provider id',128);if(raw.mutatesBaseTerrain===true)throw new Error('local embodiment provider may not own or mutate base terrain truth');if(typeof raw.materializeLocal!=='function')throw new TypeError('local embodiment provider must expose materializeLocal');return {id,authority:String(raw.authority||'UNSPECIFIED_SOURCE_AUTHORITY'),materializeLocal:raw.materializeLocal,interiorCapability:raw.interiorCapability||null,materializeInterior:raw.materializeInterior,bounds}}
function buildGrid(objects,cellM){const cells=new Map();for(const o of objects){const x=Math.floor(o.positionM[0]/cellM),y=Math.floor(o.positionM[1]/cellM),key=x+','+y;let list=cells.get(key);if(!list){list=[];cells.set(key,list)}list.push(o.pickKey)}const frozen={};for(const [key,list] of [...cells].sort((a,b)=>a[0].localeCompare(b[0])))frozen[key]=Object.freeze(list.slice().sort());return freeze({cellSizeM:cellM,cells:frozen,cellCount:Object.keys(frozen).length})}
function materialize({camera,providers=[],profileName='desktop',profileOverride={},context={}}={}){const cam=cameraSnapshot(camera),bounds=profile(profileName,profileOverride);if(!Array.isArray(providers)||providers.length>bounds.maxProviderCount)throw new RangeError('bounded local provider list required');const modeled=[],decorative=[],unsupported=[];for(const raw of providers){const provider=normalizeProvider(raw,bounds),result=provider.materializeLocal(freeze({contract:VERSION,authority:AUTHORITY,planetId:cam.planetId,anchorToken:cam.anchorToken,currentBand:cam.currentBand,camera:cam,bounds,context:freeze({...context}),claims:freeze({providerMayMutateBaseTerrain:false,decorativeInstancesArePersistentEntities:false})}));const pack=Array.isArray(result)?{objects:result}:result||{};if(pack.unsupported)unsupported.push(freeze({providerId:provider.id,reason:String(pack.unsupported)}));const objects=Array.isArray(pack.objects)?pack.objects:[],decorations=Array.isArray(pack.decorations)?pack.decorations:[];for(let i=0;i<objects.length;i++){const normalized=normalizeModeled(objects[i],provider,i,cam);if(normalized)modeled.push(normalized)}for(let i=0;i<decorations.length;i++){const normalized=normalizeDecorative(decorations[i],provider,i,cam);if(normalized)decorative.push(normalized)}}modeled.sort((a,b)=>distance2D(a.positionM,cam.absolutePresentationPositionM)-distance2D(b.positionM,cam.absolutePresentationPositionM)||a.id.localeCompare(b.id));decorative.sort((a,b)=>(mix32(hash32(a.id))-mix32(hash32(b.id)))||a.id.localeCompare(b.id));const keptModeled=modeled.slice(0,bounds.maxModeledObjects),keptDecorative=decorative.slice(0,bounds.maxDecorativeInstances),objects=Object.freeze([...keptModeled,...keptDecorative]),pickables=Object.freeze(objects.filter(o=>o.persistentEntity).slice(0,bounds.maxPickTargets).map(o=>o.pickKey)),occluders=Object.freeze(objects.slice(0,bounds.maxOccluders).map(o=>o.pickKey)),spatialIndex=buildGrid(objects,bounds.gridCellM);return freeze({version:VERSION,authority:AUTHORITY,planetId:cam.planetId,anchorToken:cam.anchorToken,currentBand:cam.currentBand,camera:cam,objects,pickables,occluders,spatialIndex,unsupported:unsupported.slice(0,bounds.maxProviderCount),resources:{profile:profileName,bounds,modeledObjects:keptModeled.length,decorativeInstances:keptDecorative.length,droppedModeled:Math.max(0,modeled.length-keptModeled.length),droppedDecorative:Math.max(0,decorative.length-keptDecorative.length),pickTargets:pickables.length,occluderCount:occluders.length,gridCells:spatialIndex.cellCount},claims:{canonicalIdentityOwned:false,canonicalSelectionOwned:false,canonicalCameraOwned:false,canonicalGeodesyOwned:false,decorativeInstancesArePersistentEntities:false}})}
function resolveInterior(provider,target,context={}){if(!provider||typeof provider!=='object')return freeze({supported:false,reason:'INTERIOR_PROVIDER_MISSING',authority:AUTHORITY});if(provider.interiorCapability!=='SUPPORTED'||typeof provider.materializeInterior!=='function')return freeze({supported:false,reason:'INTERIOR_UNSUPPORTED_BY_PROVIDER',authority:AUTHORITY});const result=provider.materializeInterior(freeze({target,context,claims:{genericInteriorSynthesisForbidden:true}}));if(!result)return freeze({supported:false,reason:'INTERIOR_PROVIDER_RETURNED_UNSUPPORTED',authority:AUTHORITY});return freeze({supported:true,authority:AUTHORITY,sourceAuthority:String(result.authority||provider.authority||'UNSPECIFIED_SOURCE_AUTHORITY'),result,claims:{genericInteriorSynthesized:false}})}
O.v2x07LocalSpatialEmbodiment=Object.freeze({VERSION,AUTHORITY,PROFILES,profile,hash32,mix32,cameraSnapshot,materialize,resolveInterior});
})(typeof globalThis!=='undefined'?globalThis:this);
