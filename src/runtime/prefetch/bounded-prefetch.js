(function(root){'use strict';const O=root.OFU=root.OFU||{},C=O.pxContracts,R=O.v2x01Contracts,V='ofu-v2x01-bounded-prefetch-1';function create(o={}){const mc=R.integer(o.maxCandidates??64,'prefetch candidates',1,512),mr=R.integer(o.maxRequests??8,'prefetch requests',1,64);C.assert(mr<=mc,'BUDGET','prefetch');const m={plans:0,candidates:0,selected:0,dropped:0};function plan(x){C.keys(x,['intentSnapshot','candidates']);C.data(x.intentSnapshot,{bytes:16384,nodes:512});C.assert(Array.isArray(x.candidates)&&x.candidates.length<=mc,'BUDGET','prefetch candidates');m.plans++;m.candidates+=x.candidates.length;const a=x.candidates.map(y=>{C.keys(y,['request','priority','distanceHint']);const q=C.data(y.request,{bytes:65536,nodes:4096});R.taskClass(q.taskClass);R.state(q.targetState);C.assert(q.taskClass!=='INTERACTION'&&q.targetState!=='IMMEDIATE','AUTHORITY','prefetch cannot own interaction');C.assert(Number.isSafeInteger(y.priority)&&y.priority>=0,'BUDGET','priority');C.assert(Number.isFinite(y.distanceHint)&&y.distanceHint>=0,'BUDGET','distance');return{q,p:y.priority,d:y.distanceHint,k:R.logicalIdentityKey(q.durable.identity)}}).sort((a,b)=>b.p-a.p||a.d-b.d||a.k.localeCompare(b.k));const s=a.slice(0,mr).map(x=>x.q);m.selected+=s.length;m.dropped+=a.length-s.length;return Object.freeze(s)}function snapshot(){return C.data({version:V,limits:{maxCandidates:mc,maxRequests:mr},metrics:m})}return Object.freeze({VERSION:V,plan,snapshot})}O.v2x01BoundedPrefetch=Object.freeze({VERSION:V,create})})(globalThis);