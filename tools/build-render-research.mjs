import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';
const root=process.cwd(),dist=path.join(root,'dist','render-research');fs.mkdirSync(dist,{recursive:true});const read=p=>fs.readFileSync(path.join(root,p),'utf8').replace(/\r\n/g,'\n'),sha=b=>crypto.createHash('sha256').update(b).digest('hex');
const files=['src/kernel/sha256.js','src/kernel/p2-unicode.js','src/kernel/p2-canonical.js','src/domains/astronomy/p3-skeleton.js','src/domains/astronomy/p3-canonical.js','src/temporal/p4-temporal.js','research/rendering/multiscale-renderer.js'];
const parts=files.map(p=>({path:p,source:read(p)}));const sourceCommit=process.env.OFU_SOURCE_SHA||'LOCAL-UNPINNED';
const boot=`(function(){'use strict';globalThis.OFU=globalThis.OFU||{};const C=${JSON.stringify(parts)};for(const c of C)(0,eval)(c.source+'\\n//# sourceURL=ofu://'+c.path);const O=globalThis.OFU,A=O.p3Astronomy,R=O.renderResearch,seed=Uint8Array.from({length:32},(_,i)=>i),ctx={masterSeed:seed,semanticManifestHash:A.semanticManifestHash()},galaxyKey={x:48n,y:-50n,z:-1n},systemKey={galaxyX:48n,galaxyY:-50n,galaxyZ:-1n,sectorX:0n,sectorY:0n,sectorZ:0n,siteX:8n,siteY:0n,siteZ:0n},planetKey={...systemKey,orbitSlot:0n};let session=R.createSession(ctx,{maxCacheEntries:160});const q=id=>document.getElementById(id),canvas=q('view');function keyFor(s){return s==='ASTRONOMICAL_OVERVIEW'||s==='GALAXY_REGION'?galaxyKey:{...planetKey,systemKey,surfaceLevel:4}}function run(){const scenario=q('scenario').value,t0=performance.now(),scene=R.plan(session,scenario,keyFor(scenario)),materializeMs=performance.now()-t0,r=R.renderWebGL2(canvas,scene),witness=R.canonicalWitness(ctx,planetKey);const report={status:r.backend==='webgl2'?'READY':'FALLBACK',sourceCommit:${JSON.stringify(sourceCommit)},scenario,capabilities:R.probe(),canonicalPlanetDigest:witness,queryCount:scene.queryCount,totalQueries:scene.totalQueries,materializedObjects:scene.materializedObjects,cache:scene.cache,materializeMs,renderer:r,surfaceAuthority:scenario==='SURFACE_LOCAL'?'PRESENTATION_ONLY':'CANONICAL_FACTS_PRESENTED'};q('out').textContent=JSON.stringify(report,null,2);globalThis.__OFU_RENDER_RESEARCH_REPORT__=report;return report}q('run').onclick=run;q('clear').onclick=()=>{session.cache.clear();run()};globalThis.__OFU_RENDER_RESEARCH_CONTROL__={run,reset(){session=R.createSession(ctx,{maxCacheEntries:160});return run()},loseRestoreContext(){const gl=canvas.getContext('webgl2'),ext=gl&&gl.getExtension('WEBGL_lose_context');if(!ext)return false;ext.loseContext();setTimeout(()=>ext.restoreContext(),0);return true}};run()})();`;
const template=read('research/rendering/research-template.html');const html=template.replace('__SCRIPT__',boot),out=path.join(dist,'One_File_Universe_Render_Research.html');fs.writeFileSync(out,html);const buf=fs.readFileSync(out),manifest={status:'PASS',sourceCommit,artifact:path.basename(out),artifactBytes:buf.length,artifactSha256:sha(buf),strictSingleFile:true,directFile:true,offline:true,networkRequired:false,webgl2Baseline:true,webgpuRequired:false,cacheBound:160,components:parts.map(x=>({path:x.path,sha256:sha(x.source),bytes:Buffer.byteLength(x.source)}))};fs.writeFileSync(path.join(dist,'render-research-build-manifest.json'),JSON.stringify(manifest,null,2)+'\n');console.log(JSON.stringify(manifest,null,2));
