import { assertRecord,int,mulDivFloor,nonNegativeInt,ppm } from '../reference/bounded-math.mjs';import { CIV_AUTHORITY } from './production.mjs';
export function coordinationCapacity(input){assertRecord(input,'input');const institution=ppm(input.institutionCapacityPpm??0),cooperation=ppm(input.cooperationPpm??0),conflict=ppm(input.conflictLoadPpm??0),retained=mulDivFloor(institution,1_000_000-conflict,1_000_000),gain=mulDivFloor(1_000_000-retained,cooperation,1_000_000);return Object.freeze({authority:CIV_AUTHORITY,effectiveCapacityPpm:Math.min(1_000_000,retained+gain),sociologyClaim:false});}
export function collapseRecoveryStep(state,forcing,policy){assertRecord(state,'state');assertRecord(forcing,'forcing');assertRecord(policy,'policy');const reserve=nonNegativeInt(state.reserve??0,'reserve',1_000_000_000),delta=int(forcing.reserveDelta??0,'reserveDelta',-1_000_000_000,1_000_000_000),nextReserve=Math.max(0,Math.min(1_000_000_000,reserve+delta)),collapse=nonNegativeInt(policy.collapseThreshold,'collapseThreshold',1_000_000_000),recovery=nonNegativeInt(policy.recoveryThreshold,'recoveryThreshold',1_000_000_000),need=nonNegativeInt(policy.requiredRecoveryTicks??1,'requiredRecoveryTicks',1_000_000);if(recovery<collapse)throw new Error('recoveryThreshold must be >= collapseThreshold');let mode=state.mode??'OPERATING',ticks=nonNegativeInt(state.recoveryTicks??0,'recoveryTicks',1_000_000);if(mode==='OPERATING'&&nextReserve<collapse){mode='COLLAPSED';ticks=0;}else if(mode==='COLLAPSED'){ticks=nextReserve>=recovery?ticks+1:0;if(ticks>=need){mode='OPERATING';ticks=0;}}return Object.freeze({authority:CIV_AUTHORITY,reserve:nextReserve,mode,recoveryTicks:ticks,nonClaim:'Systems abstraction, not prediction of societal collapse.'});}
