import { assertRecord,boundedArray,mulDivFloor,ppm,uniqueIds } from '../reference/bounded-math.mjs';import { CIV_AUTHORITY,CIV_LIMITS } from './production.mjs';
export function advanceInfrastructure(assets,forcing){boundedArray(assets,'assets',CIV_LIMITS.routes+CIV_LIMITS.institutions);uniqueIds(assets,'assets');assertRecord(forcing,'forcing');const hazard=ppm(forcing.hazardPpm??0);return Object.freeze(assets.map(a=>{if(typeof a.originEventId!=='string'||!a.originEventId)throw new Error(`asset ${a.id} lacks originEventId`);const condition=ppm(a.conditionPpm??1_000_000),decayPpm=ppm(a.decayPpm??0),maintenance=ppm(a.maintenancePpm??0),decay=Math.min(condition,mulDivFloor(condition,decayPpm+Math.min(1_000_000-decayPpm,hazard),1_000_000)),after=condition-decay,restoration=mulDivFloor(1_000_000-after,maintenance,1_000_000);return Object.freeze({...a,conditionPpm:Math.min(1_000_000,after+restoration),evidence:Object.freeze({decay,restoration,originEventId:a.originEventId})});}));}
export function archaeologicalTrace(asset,context){assertRecord(asset,'asset');assertRecord(context,'context');if(typeof asset.originEventId!=='string'||!asset.originEventId)return Object.freeze({authority:CIV_AUTHORITY,state:'UNSUPPORTED_NO_CAUSAL_ORIGIN',trace:null});const retained=Math.max(0,Math.min(1_000_000,Math.floor((ppm(context.materialDurabilityPpm??0)+ppm(context.burialProtectionPpm??0)+ppm(asset.conditionPpm??0)-ppm(context.exposureDamagePpm??0))/2)));return Object.freeze({authority:CIV_AUTHORITY,state:retained>0?'MODEL_TRACE_PERSISTS':'MODEL_TRACE_NOT_PRESERVED',trace:Object.freeze({originEventId:asset.originEventId,assetId:asset.id,preservationScorePpm:retained}),empiricalDatingClaim:false,nonClaim:'Scenario preservation abstraction, not calibrated archaeology.'});}
