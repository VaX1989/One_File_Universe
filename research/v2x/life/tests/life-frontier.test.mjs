import test from 'node:test'; import assert from 'node:assert/strict'; import { stepEcology, thermodynamicOpportunity } from '../energetics.mjs'; import { deterministicVariant, evaluateSpeciationWitness, selectionStep, validateInnovationGraph } from '../evolution.mjs'; import { routePropagules, successionEligibility } from '../landscape.mjs';
test('thermodynamic opportunity never infers life',()=>{const out=thermodynamicOpportunity({donorAvailable:true,acceptorAvailable:true,freeEnergyGradientProxy:12,pathwayAuthority:'SOURCE_BACKED'});assert.equal(out.state,'SOURCE_BACKED_OPPORTUNITY');assert.equal(out.canInferLife,false);});
test('ecology conserves resource ledger and responds to shock',()=>{const state={resources:[{id:'r',stock:1000,inflow:0,lossPpm:0}],guilds:[{id:'a',resourceId:'r',population:10,reserve:0,maxUptakePerCapita:10,maintenancePerCapita:8,assimilationPpm:800000,reproductionCost:20,baselineMortalityPpm:0},{id:'b',resourceId:'r',population:10,reserve:0,maxUptakePerCapita:10,maintenancePerCapita:8,assimilationPpm:800000,reproductionCost:20,baselineMortalityPpm:0}]};const control=stepEcology(state);const shock=stepEcology(state,{resourceDelta:{r:-950}});assert.equal(control.resources[0].consumed+control.resources[0].stock+control.resources[0].abioticLoss,1000);assert.equal(shock.resources[0].consumed+shock.resources[0].stock+shock.resources[0].abioticLoss,50);assert.ok(shock.guilds.reduce((s,g)=>s+g.population,0)<control.guilds.reduce((s,g)=>s+g.population,0));});
test('selection and extinction are criterion driven',()=>{const out=selectionStep([{id:'near',population:1000,trait:100},{id:'far',population:1000,trait:900}],{optimumTrait:100,viabilityWidth:500},{birthPpm:200000,deathPpm:100000});assert.ok(out[0].population>out[1].population);assert.equal(selectionStep([{id:'x',population:1,trait:1000}],{optimumTrait:0,viabilityWidth:1},{birthPpm:0,deathPpm:1000000})[0].extinct,true);});
test('mutation deterministic and speciation remains non-claim',()=>{const a=deterministicVariant({id:'lin',trait:100},7,{maxMutationStep:20,salt:'s'});assert.deepEqual(a,deterministicVariant({id:'lin',trait:100},7,{maxMutationStep:20,salt:'s'}));const w=evaluateSpeciationWitness({population:100,reproductiveIsolationProxyPpm:900000,persistenceGenerations:100,traitDivergence:500},{minPopulation:50,minIsolationPpm:800000,minPersistenceGenerations:10,minTraitDivergence:100});assert.equal(w.satisfied,true);assert.equal(w.biologicalSpeciesClaim,false);assert.throws(()=>deterministicVariant({trait:100},1,{maxMutationStep:1}),/lineage.id/);});
test('innovation DAG rejects cycles',()=>{assert.equal(validateInnovationGraph([{id:'a',requires:[]},{id:'b',requires:['a']}]),true);assert.throws(()=>validateInnovationGraph([{id:'a',requires:['b']},{id:'b',requires:['a']}]),/cycle/);});
test('dispersal capacity and succession nonclaim',()=>{const out=routePropagules({patches:[{id:'p0'},{id:'p1'}],edges:[{id:'e',from:'p0',to:'p1',capacity:10,survivalPpm:500000}]},[{id:'m',edgeId:'e',count:20}]);assert.equal(out.edgeUse.e,10);assert.equal(out.arrivals.p1,5);const s=successionEligibility({disturbanceAgeTicks:5,resourceIndex:100},{minDisturbanceAgeTicks:2,minResourceIndex:80});assert.equal(s.eligible,true);assert.equal(s.successionStageClaim,false);});
