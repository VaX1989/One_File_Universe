# V1X-19 matter / molecular / atomic research prototype

Status: **RESEARCH ONLY / MODEL_DERIVED_SIMULATION CANDIDATE**. Nothing here has canonical or shipping authority.

Frozen input base: `760c0c70bccb6afd7c7b07600c0f5be3f80e7b19` / tree `3324e6354b5ff1b90156d1693e6051595b6f1219`; contract set `OFU-V1X-CONTRACT-SET-2026-09-06.1`.

The frozen base already contains a conservative material → microstructure → molecular → atomic ladder. This research lane adds an extraction-oriented model for explicit regime validity, source authority, deterministic microscopic identity, fixed-point reference properties, bounded normalized atom sites, resource witnesses, return-context witnesses, and a hard non-classical boundary.

Non-negotiable semantics:

- semantic regime changes are not claimed to be literal geometric zooms (`geometricZoomClaim:false`);
- computed DFT structures cannot be upgraded to experimental authority;
- unresolved chemistry remains unresolved rather than fabricating molecules/atoms;
- molecular/atomic outputs never claim exact bulk inventory or trajectories;
- experimentally sourced, computed and presentation-only coordinates stay distinct;
- upstream organism/tissue/cell/object context is preserved but never invented;
- standard atomic weights use decimal Q9 fixed-point / BigInt arithmetic;
- normalized state-facing coordinates use integer femtometres and require explicit upstream quantization policy;
- the full canonical key is identity; the 64-bit checksum is only a non-cryptographic acceleration hint;
- resource byte counts are estimates, not measured runtime evidence;
- electronic refinement stops at an explicit non-classical boundary with no electron-orbit fiction.

Files: `microphysics-prototype.mjs`, `data/atomic-reference-2024.json`, `fixtures/source-contexts.json`, `tests/microphysics-prototype.test.mjs`, and `SCIENTIFIC_CONTRACT.md`.
